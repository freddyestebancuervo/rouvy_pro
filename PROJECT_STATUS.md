# Korixa — Estado del Proyecto

> **Nombre del producto:** el producto se llama actualmente **Korixa**. El repositorio (`freddyestebancuervo/rouvy_pro`) y varios identificadores técnicos heredados de su nombre anterior, **RidePro**, siguen vigentes y **no han sido renombrados**: nombre de repositorio (`rouvy_pro`), namespace de Android (`com.ridepro.app`; Production usa el `applicationId` placeholder `com.ridepro.app.YOUR_APPLICATION_ID`, todavía pendiente de resolver — ver `android/app/build.gradle.kts`; Development usa el `applicationId`/package real `com.ridepro.app.dev`), archivos como `RIDEPRO_DEVELOPMENT_PROTOCOL.md`, y referencias internas en código/configuración/documentación histórica. **El Bundle ID de iOS sí fue renombrado**: `com.korixa.app` en Production (PR #32, merge `4cca134dd16509fa48bdc511083595aa27a234ad`) y `com.korixa.app.dev` en Development (PR #34, merge `f1646958612dd9b0054d457c030509ab70741988`) — ver historial, sección 5. Este documento usa "Korixa" para referirse al producto y conserva "RidePro"/`rouvy_pro` donde cita un identificador técnico real, sin alterarlo. No se ha ejecutado ni se ha autorizado una migración masiva de nombre — ver `docs/product/PRODUCT_IDEAS_REGISTRY.md` para el ecosistema de nombres de producto (Ride, Studio, Coach, Connect, Receiver, Race, Cloud, Creator) registrado como idea/visión, no como hecho ya implementado.

> Documento vivo. Se actualiza en la Etapa 10 (Cierre) de toda tarea, el mismo día en que se cierra, según `RIDEPRO_DEVELOPMENT_PROTOCOL.md` §6. Nunca se reescribe el historial de cambios — solo se agregan entradas nuevas al final. Fuente de los datos iniciales: `docs/audits/AUDITORIA_FINAL/MASTER_EXECUTION_PLAN.md` y `BACKLOG_MAESTRO.md` — cero hallazgos nuevos generados al crear este documento.

- **Fecha de última actualización:** 2026-08-24
- **Actualizado por:** Sincronización documental post-PR #88 — reconciliación de `T-F1.2` sobre `main`=`caf4afbfbf70efc6306ee9bd83ff8f48feb0f599`: PR #85 y PR #86 fusionados; PR #87 permanece Draft/abierto y queda superado por el hardening sistémico de PR #88 (sin cerrarlo en esta tarea); PR #88 fusionado tras B PASS/C PASS y Human Gates separados, con CI post-merge run `32801653025` 4/4 `SUCCESS` y Post-merge CI locator run `32801868448` `SUCCESS`. `T-F1.2` permanece **EN PROGRESO** y `MIGRATION_PRECONDITION_PROVEN=NO`. Esta sincronización es exclusivamente documental: no modifica código/workflows, no ejecuta Production y no cierra PR #87 ni PR #81.
- **Commit base de `main` auditado al iniciar este bloque:** `caf4afbfbf70efc6306ee9bd83ff8f48feb0f599` (merge commit de PR #88; CI post-merge 4/4 `SUCCESS`, run `32801653025`)
- **Rama de referencia:** `main`. `feature/d2` está completamente contenida/superada por `main` (0 commits propios no incluidos en `main`) y se conserva únicamente como referencia histórica; no es una rama activa ni debe usarse como base para trabajo nuevo. Toda nueva implementación parte del HEAD vigente de `origin/main` en una rama aislada, salvo excepción explícita.

---

## 0. ESTADO VIGENTE — LEER PRIMERO

> Esta sección es la fuente de verdad operativa inmediata.
> Las entradas históricas posteriores se conservan por trazabilidad y
> describen correctamente el estado de su fecha, pero una afirmación
> histórica que contradiga esta tabla queda SUPERADA por evidencia posterior.
> Ningún agente debe reabrir, reimplementar o volver a ejecutar una tarea
> marcada **CERRADA** aquí basándose únicamente en una frase histórica aislada.
>
> **CORRECCIÓN VIGENTE — `T-F1.2` POST-PR #88 (2026-08-24):** este bloque tiene precedencia sobre cualquier detalle incompatible de la fila histórica extensa de `T-F1.2`, la fila "Production backend", el apartado "Próximo camino operativo" y otras frases punto-en-el-tiempo de este documento. **PR #85** fusionó el caller manual-only de deploy de Production (`workflow_dispatch`) y conectó la cadena `guard → build-production-artifact → verify-build-output → deploy-production-candidate`, sin deploy real. **PR #86** fusionó el caller manual-only de inspección read-only de la base de datos (`guard → prepare-inspection-inputs → build-production-artifact → verify-build-output → inspect-production-db`), sin ejecutar la inspección durante el PR. El primer intento posterior de dispatch de esa inspección fue rechazado por GitHub **antes de crear un run** porque jobs normales con `steps:` carecían de `runs-on:`; por tanto no corrió ningún job, no hubo conexión/consulta DB y hubo **cero mutaciones de Production**. **PR #87** fue el fix inicial acotado y permanece Draft/abierto, pero quedó **SUPERADO/SUPERSEDED** técnicamente por **PR #88**, que corrigió la clase de defecto y endureció la validación estructural/actionlint, la certificación B/C y el Human Gate; #88 fue fusionado a `main`=`caf4afbfbf70efc6306ee9bd83ff8f48feb0f599`, y su CI post-merge real (`32801653025`) terminó 4/4 `SUCCESS`; el locator post-merge (`32801868448`) también terminó `SUCCESS`. **Estado operativo actual:** `T-F1.2 = EN PROGRESO`, `MIGRATION_PRECONDITION_PROVEN = NO`, no existe evidencia de un deploy real de Cloud Run Production y los callers de Production siguen manuales, por lo que el criterio oficial de automatización end-to-end todavía no se cumple. **Siguiente paso técnico:** preflight de solo lectura del workflow de inspección DB sobre el `main` vigente; solo si ese preflight pasa, cualquier dispatch requiere autorización humana separada.

| Área / tarea | Estado vigente | Evidencia / nota |
|---|---|---|
| `T-F0.1` (crash de Wearables en Flutter Web) | **CERRADA** | Implementación (PR #9, merge `0582e4933ef9f1e5ab0fce8f18197c6ffb2c7614`): guarda de plataforma Web antes de evaluar `dart:io Platform` en `HealthPlatformGatewayImpl.checkAvailability()`, con test de regresión automatizado. La validación manual complementaria en navegador real (protocolo §1, Etapa 5/Puerta 3), pendiente en la entrada histórica de esta tabla, se completó en una sesión posterior (Task17): smoke real Web→Wearables sobre `main`=`67ca052c6304e29a00aaffbcd2268693d1374ca5` — Apple Health y Google Fit mostraron "no disponible en este dispositivo", sin `UnsupportedError`, sin crash de `Platform`, sin pantalla roja. La implementación de PR #9 sigue presente en `main` actual (re-verificado en Task18/QG0, 2026-08-17). |
| `T-F0.2` / `C1` (separación de entornos Firebase) | **CERRADA** | Declarada formalmente cerrada el 2026-08-15 — las 10 puertas del Documento 15 §12 (A-J) están **Cumplida** para Development. Cierre operacional de la Puerta H: PR #43 (merge `c2b6f260d2b79a49c8db335991b09abdea6a56ab`) + primer deploy automatizado real a Cloud Run Development (`workflow_dispatch` run `31924059938`, `SUCCESS`). Cierre documental/formal de `T-F0.2`/`C1`: PR #44 (merge `d26feddca9a0b0a75820a4988cd98e4f7b0e990a`). Ver historial 2026-08-15. Re-verificado de forma independiente en Task18/QG0 (2026-08-17). **No reabrir** sin `REOPEN_REASON`/`NEW_EVIDENCE`/`LAST_CLOSING_EVIDENCE`/`WHY_CLOSURE_IS_INVALID` explícitos. |
| `T-F0.3` (`docker-compose.yml` local) | **CERRADA** | Integrada a `main` — PR #29 (merge `2d8e5751931c2a76a300dcaf7a7ee812ad19a2d0`). Re-verificado con `docker compose up` real en Task18/QG0 (2026-08-17): Postgres healthy → migraciones → claves JWT de desarrollo → Redis → backend healthy → `GET /v1/health` → `200 {"status":"ok","database":"connected"}`, sin pasos manuales. |
| `T-F0.4` (rate limiter → Redis) | **CERRADA** | Integrada a `main` — PR #30 (merge `ce54cb5fd769177e32a4fa3feb8d144bee1d91b5`): `RedisThrottlerStorage` como storage del throttler, con `backend/test/rate-limit-multi-instance.e2e-spec.ts` validando dos instancias Nest independientes compartiendo el mismo contador vía Redis. Re-verificado en Task18/QG0 (2026-08-17). |
| `T-F0.5` (paginación real en `equipment`/`workouts`) | **CERRADA** | Fase Enforcement — PR #55 (merge `22e7dda9f8cc12c2e644f0449cd7a7c0b1583058`): `GET /equipment`/`GET /workouts` enrutan siempre por `listPaginated()`, con `DEFAULT_LIMIT=50`/`MAX_LIMIT=100` aplicados incluso sin `limit`/`cursor`; body siempre `ARRAY`; Flutter Workouts consume todas las páginas vía `X-Next-Cursor`. CI post-merge: run `32042395180` (Flutter/Firestore/Backend) y `32042395169` (Docker build), ambos `PASS` sobre el merge commit exacto. Re-verificado en Task18/QG0 (2026-08-17). |
| Android Production / Bloque 5C (`applicationId` placeholder) | **PENDIENTE — propuesto, no autorizado** | `com.ridepro.app.YOUR_APPLICATION_ID` sigue sin corregir en Production. Decisión de reemplazo (`com.ridepro.app`) ya aprobada (Documento 15, D3) pero nunca ejecutada. Requiere registrar una app Android nueva en `ridepro-dbafe` — no iniciar sin autorización explícita. |
| `QG0` (Documento `MASTER_EXECUTION_PLAN.md` §9) / Task18 | **CERTIFICADA — PASS** | Auditoría de solo lectura (Task18, 2026-08-17) sobre `main`=`22e7dda9f8cc12c2e644f0449cd7a7c0b1583058`: `T-F0.1`–`T-F0.5` = `PASS_CLOSED`; gate de regresión del `main` actual (CI post-merge PR #55 + Docker build) = `PASS`; sin bloqueos. Task18 no modificó código ni infraestructura por sí misma. |
| `T-F1.1` (elegir plataforma de hosting del backend) | **CERRADA** | El propietario tomó la decisión formal el 2026-08-17: **Google Cloud Run** como plataforma de hosting del backend para Production. La decisión ocurrió después de un análisis comparativo Cloud Run / Render / VPS (compatibilidad arquitectónica, seguridad/ADC/WIF, reutilización del pipeline y de Artifact Registry ya existentes, escalabilidad, carga operativa), en el que Cloud Run también fue la recomendación técnica. Development ya operaba de facto sobre Cloud Run/Cloud SQL, pero esa realidad previa **no fue tomada como autorización automática** — la selección para Production es una decisión explícita y posterior del propietario, distinta de esa realidad de Development. `T-F1.1` queda cerrada porque el criterio de aceptación oficial ("plataforma de hosting seleccionada y documentada", `BACKLOG_MAESTRO.md`) queda satisfecho. **Esto NO significa que Production ya esté desplegado**: no existe todavía Cloud Run/Cloud SQL/Redis de Production, ni pipeline de Production — ver filas "Production backend" y "Staging". |
| `T-F1.2` (pipeline de CD para Production) | **EN PROGRESO** | Iniciada formalmente por PR #58 (Block A, workflows inertes). `D1E1` (impersonation WIF↔Service Accounts) cerrado en sesión operativa 2026-08-18 (ver historial). Desde entonces, 4 bloques adicionales cerrados con evidencia directa de PR/CI/Actions: **`D1F1`** — PR #60 (merge `90e36f621ff0728323d570491795fbee`), activa el camino versionado de smoke WIF auth-only para Production (sin dispatch en ese PR). **`D2B1`** — PR #61 (merge `1652c3fd483a6ba9508e5e638e0c3c28a305629a`), agrega la operación `build-publish` (gated) al reusable de Production, publicando exclusivamente a `southamerica-east1-docker.pkg.dev/ridepro-dbafe/korixa-backend/api` con tags de SHA completo, provenance OCI y fail-closed ante colisión de tag inmutable. **`D2B2`** — PR #62 (merge `1ba4e88f9f826f33bc1a45ec98ff2a44c90ea845`), corrige un bug de causa raíz descubierto en la primera ejecución real (run `32192988308`, falló): el precheck de colisión de tag usaba `gcloud ... describe` buscando la subcadena `NOT_FOUND`, pero el texto real de gcloud es `"Image not found."` — reemplazado por `gcloud artifacts docker images list --include-tags --format=json` con stdout/stderr en archivos separados. **`D2B3`** — PR #63 (merge `6fd5ff023d767079d7a5a0e724161190971dab71`), sube el runtime del backend de Node 20 a Node 24 (Docker + CI), con 170 unit + 131 e2e + 28 reglas de Firestore en verde y `npm audit` runtime en HIGH=0/CRITICAL=0. **Prueba real de ejecución exitosa (evidencia directa de Actions):** el run `32210700072` (2026-08-19T03:02 UTC, operación `build-publish`, tras el fix de `D2B2`) completó `success` y **publicó una imagen Docker real** en Artifact Registry de Production (capas confirmadas `Pushed` en el log) usando WIF con la Service Account `korixa-artifact-publisher`, sin credenciales de larga duración. **`_backend-deploy-cloud-run-production.yml` ("Backend — Production deploy bootstrap") permanece con 0 ejecuciones — el despliegue real a Cloud Run de Production todavía NO se ha ejecutado.** **Reconciliación de infraestructura real (2026-08-22, este bloque):** un preflight de solo lectura (`gcloud ... --project=ridepro-dbafe` explícito) descubrió que la infraestructura de datos/identidad de Production **ya está provisionada y operativa**, sin que este documento lo reflejara hasta ahora — ver la fila "Production backend" para el detalle completo. Esto no avanza `T-F1.2` por sí mismo (ningún workflow ejecutó estas mutaciones; fueron creadas en sesión operativa fuera de Git, mismo patrón ya usado para `D1E1`) — solo corrige un vacío documental. **Bloque `D3B0` — implementación, doble remediación y fusión del reusable de despliegue (2026-08-22, PR #68, este bloque):** `_backend-deploy-cloud-run-production.yml` dejó de ser un stub de solo contrato — **está implementado e integrado a `main`** (PR #68, 3 commits: implementación inicial `0f95d48`, remediación 1 `b3b6182`, remediación 2 `5a35779`; merge `7a5ec01d2dd99b5cef2188ee9e937ff633c9d342`, 2 padres `235b030e`/`5a35779`; CI post-merge 4/4 `PASS`, run `32582352624`). Pasó por dos auditorías independientes (inicial: `PASS_WITH_FINDINGS` P0=1/P1=4/P2=4/P3=2, íntegramente remediado; re-audit adversarial posterior a la remediación: `PASS_WITH_FINDINGS` P0=0/P1=0/P2=7, los P2 resolubles por código cerrados en remediación 2) antes de un precheck final `PASS` y fusión ordinaria (sin squash/rebase/bypass). Propiedades de seguridad verificadas por código: descubrimiento de "primer despliegue" fail-closed (nunca infiere inexistencia de un `describe` fallido); artefacto vinculado por digest inmutable, con procedencia OCI (`source_sha`) verificada ANTES del deploy y con igualdad estricta `image == immutable_ref` verificada DESPUES (dos capas independientes); despliegue `--no-traffic` con health check de la candidata antes de cualquier switch; Direct VPC Egress hacia `korixa-production-vpc`; secretos JWT montados como archivo, `DATABASE_URL`/`REDIS_URL` como env vars respaldadas por secreto; gate de migración puramente declarativo (nunca ejecuta nada); **el primer despliegue de un servicio nuevo NUNCA mueve tráfico automáticamente** — el job de switch se omite entero, la candidata queda creada/sana en 0%, y promoverla es siempre un bloque futuro separado y explícitamente autorizado; los despliegues subsecuentes tienen rollback que **verifica** su propio resultado (split de tráfico + health post-restauración), no que lo asume; guarda de `concurrency` a nivel de workflow; `environment: production` ya conectado en los dos jobs que tocan cloud (hoy sin efecto — el Environment no tiene reviewers obligatorios configurados). **El reusable sigue completamente INALCANZABLE**: `production-deploy.yml` no lo invoca (verificado por diff byte-a-byte contra su estado en `main`, sin cambios), cero referencias `uses:` a este archivo en todo el repositorio, cero triggers `workflow_run`/`push`/`schedule` apuntando a Production — fusionar PR #68 no pudo, por sí mismo, desplegar ni mutar nada de Production, y de hecho no lo hizo. **PR #69** (merge `316dbb36a33bf8f0dce0033d54f80248d9d4c9a1`) reconcilió este mismo documento tras PR #68 — trabajo exclusivamente documental. **NONPROD TEST A — semántica de invocación por defecto (2026-08-22):** contra un servicio Cloud Run nuevo y desechable en `ridepro-development` (proyecto no-Production, identidades desechables creadas y eliminadas para el test, nunca usadas contra `ridepro-backend-dev` ni Production), con un deployer temporal cuyo límite de privilegio igualó materialmente al deployer real de Production (`roles/run.developer`, sin `run.services.setIamPolicy`), un deploy que omite ambos flags de invocación (`--allow-unauthenticated`/`--no-allow-unauthenticated`) resultó: deploy exitoso, política IAM resultante sin binding `allUsers`→`run.invoker`, petición anónima `403`, petición autenticada `200` — comportamiento **DEFAULT_PRIVATE**, `PROVEN_BY_LIVE_NONPROD_TEST`. Auditoría independiente: `PASS_WITH_FINDINGS`, único hallazgo: la contabilidad de mutaciones IAM/identidad temporales del reporte original decía "4", la reconstrucción independiente vía Cloud Audit Logs confirmó 8 operaciones reales — clasificado `REPORTING_ACCURACY ONLY` (estado permanente neto = 0, verificado). Limpieza completa verificada de forma independiente (servicio, ambas Service Accounts temporales, bindings IAM — todo eliminado, cero residuos). Cero efectos sobre Production o sobre `ridepro-backend-dev`. Disposición: **`INVOCATION_FLAG_OMISSION = RESOLVED_PROVEN`** — evidencia puntual bajo el límite de privilegio ensayado/materialmente equivalente a Production tal como existía el 2026-08-22, no una garantía de que un cambio futuro de IAM/política de organización no pueda alterar este comportamiento. **NONPROD TEST B — semántica de reintento con el mismo tag (2026-08-22):** mismo entorno desechable. R1 (100% de tráfico primario) → R2 (candidata nueva, 0%, con tag) → repetir EXACTAMENTE la misma configuración de deploy con el mismo tag (B1): creó una revisión nueva y reasignó el tag de forma atómica a esa revisión nueva; la candidata anterior quedó sin tag y en 0%, nunca duplicada, nunca ambigua; el tráfico primario permaneció en 100% durante toda la secuencia. Disposición: **`SAME_ATTEMPT_TAG_RETRY = RESOLVED_PROVEN_SAFE`** — prueba el caso de reintento EXACTO ensayado, no independencia universal de cualquier otra variación de configuración con el mismo tag. Auditoría independiente: `PASS_WITH_FINDINGS`. **Con TEST A y TEST B, los dos bloqueadores `NONPROD_EVIDENCE_REQUIRED` heredados de PR #68 quedan cerrados** — ninguna pregunta de semántica de `gcloud` permanece sin evidencia real. **TEST B también descubrió, en vivo, dos defectos P1 reales ya presentes en el reusable fusionado en PR #68** (`DISCOVERED_BY_NONPROD_TEST` → `RESOLVED_BY_PR70`, ver más abajo): (P1-A) Cloud Run exige `length(service_name) + length(candidate_tag) <= 46`, límite real nunca validado por el contrato; (P1-B) la auditoría de identity token para el health check de la candidata debe seguir siendo la URL canónica del servicio incluso al solicitar una URL etiquetada (confirmado por evidencia en vivo y por la documentación oficial de Cloud Run) — el código usaba incorrectamente la URL etiquetada como audiencia. Un hallazgo relacionado (mismo origen, revisión independiente de PR #70): el health check posterior a un rollback carecía de fallback autenticado, pudiendo reportar como fallido un rollback realmente exitoso si el servicio terminara siendo IAM-protegido. **PR #70** (`fix(ci): harden Cloud Run candidate tag and private health checks`, 1 commit, 1 archivo — el propio reusable; merge ordinario de 2 padres `316dbb36a33bf8f0dce0033d54f80248d9d4c9a1`/`d79b51794ef05e03c658b9784267841a23798be8` → `e5158cd5825fde4b8bd5f154acf96d34b7d8161d`; CI post-merge run `32588729509`, evento `push`, intento 1, 4/4 `SUCCESS`) corrigió los tres hallazgos: tag de candidata rediseñado más corto (`c-<github.run_id>-<github.run_attempt>`, ya sin `source_sha` — la procedencia se prueba de forma independiente vía etiqueta OCI + igualdad estricta de digest), calculado una única vez en `validate-contract` como fuente única, con validación fail-closed del límite combinado real (≤46) antes de checkout/OIDC/auth/cloud; el health check de la candidata ahora usa dos valores distintos — `REQUEST_URL` (URL etiquetada, sin cambios) y `TOKEN_AUDIENCE` (URL canónica `status.url`, el valor correcto); el health check posterior a rollback obtuvo el mismo fallback autenticado. Los criterios de éxito de salud (`HTTP 200` + `status=="ok"` + `database=="connected"`) permanecen sin debilitar. Reauditoría independiente de PR #70: `PASS_WITH_FINDINGS` — P0=0, P1=0, P2=0, P3=1 (`CONTRACT_VALID=YES` se imprime antes de que corra el step separado de validación de longitud combinada del tag; cero consumidores en todo el repositorio, el resultado del job permanece correctamente fail-closed pase lo que pase con ese texto — `REPORTING_ACCURACY`, `NON_BLOCKING`, no elevado a bloqueador de activación). Precheck final `PASS`, fusión ordinaria confirmada, certificación postmerge `PASS` — cero ejecuciones de Production causadas por todo este bloque (TEST A, TEST B, PR #70 completo). **El reusable permanece INALCANZABLE** tras el merge de PR #70 (mismas verificaciones de cero llamadores, repetidas y confirmadas contra el nuevo `main`). Decisión de privilegios DB de migración sigue `DEFERRED`/abierta; el gate de migración (`migration_precondition_confirmed`) es un contrato **IMPLEMENTADO**, pero la ejecución real de migraciones, la identidad dedicada de migración, y la verificación técnica del esquema de Production siguen **NO IMPLEMENTADAS**. Siguiente trabajo técnico, ninguno ejecutado todavía: diseño e implementación del flujo real de migraciones (identidad, privilegios mínimos, orden de ejecución, estrategia expand/contract, forward-fix vs. rollback, evidencia de verificación); guarda de frescura contra el `main` vigente para un futuro llamador (este reusable prueba `checkout == source_sha`, nunca `source_sha == main` actual — aceptable solo porque no existe llamador todavía); decisiones explícitas del propietario sobre el servicio (nombre, CPU, memoria, min/max instancias, `cors_allowed_origins`, política de invocación pública/IAM — la semántica de omitirla ya está `RESOLVED_PROVEN`, la decisión de producto en sí sigue pendiente —, reviewer obligatorio del GitHub Environment `production`, valor final de `vpc_egress_mode` — el mecanismo Direct VPC Egress ya está implementado, solo falta elegir `private-ranges-only` vs. `all-traffic`); política de backups automáticos de Cloud SQL de Production, hoy deshabilitados (riesgo de endurecimiento pre-tráfico, sin decisión ni corrección); diseño de un bloque futuro, separado y explícitamente autorizado, para la promoción a tráfico real de un primer despliegue (la creación y validación de salud de la candidata de primer despliegue ya está implementada; la promoción automática está **intencionalmente ausente** por diseño — `traffic-switch` se omite entero como job — no es un defecto pendiente de corregir). `T-F1.2` **no** cumple todavía su criterio de aceptación oficial ("un push a `main` despliega automáticamente al entorno correspondiente sin pasos manuales", `BACKLOG_MAESTRO.md`) — permanece abierta. |
| Staging | **NO INICIADO** | Ningún proyecto GCP/Firebase de Staging existe todavía (Documento 23). |
| Production backend | **NO DESPLEGADO (Cloud Run) — resto de la infraestructura de datos/identidad ya PROVISIONADA** | `ridepro-dbafe` conserva su configuración de cliente histórica (Auth, Android, Web, iOS); Cloud Firestore nunca fue habilitado ahí. **Artifact Registry** (`southamerica-east1-docker.pkg.dev/ridepro-dbafe/korixa-backend/api`) **operativo para build/publish** — probado con éxito real (run `32210700072`). **Verificado en vivo el 2026-08-22 (`gcloud ... --project=ridepro-dbafe`, solo lectura), infraestructura no documentada hasta este bloque:** **Cloud SQL** `korixa-production-postgres` (POSTGRES_16, `RUNNABLE`, IP privada únicamente, `southamerica-east1`) — **⚠ backups automáticos deshabilitados** (`settings.backupConfiguration.enabled=false`), observado, no corregido en este bloque. **Redis/Memorystore** `korixa-production-redis` (`REDIS_7_2`, `READY`, tier BASIC 1GB, IP privada, `authEnabled=true`). **VPC** `korixa-production-vpc` con peering privado a Cloud SQL y a Redis, ambos `ACTIVE`; `vpcaccess.googleapis.com` (conector clásico) permanece deshabilitada — el **mecanismo** de egress de Cloud Run hacia esta VPC ya está decidido e **implementado** en el reusable como Direct VPC Egress (`--network`/`--subnet`/`--vpc-egress`, PR #68, merge `7a5ec01d2dd99b5cef2188ee9e937ff633c9d342`); el **valor** concreto de `vpc_egress_mode` (`private-ranges-only` vs. `all-traffic`) sigue **sin decidir** — es un input `required: true` sin default, decisión del propietario. **Secret Manager**: 5 secretos existen (`korixa-production-database-url`, `korixa-production-redis-url`, `korixa-production-jwt-private-key`, `korixa-production-jwt-public-key`, `korixa-production-migration-database-url`); los primeros 4 ya están vinculados (`roles/secretmanager.secretAccessor`) a la identidad de runtime; el de migración no tiene ningún binding IAM a nadie — coherente con la decisión de privilegios de migración, que sigue `DEFERRED`. **Identidades**: `korixa-artifact-publisher@`, `korixa-production-deployer@` y `korixa-production-runtime@` existen con separación de funciones correcta (verificado: el deployer puede impersonar/desplegar y actuar como el runtime vía `roles/iam.serviceAccountUser`, pero no tiene acceso a secretos; el runtime no puede ser impersonado desde CI y no tiene ningún rol de proyecto) — cero llaves de larga duración (`USER_MANAGED`) en ninguna. **`GitHub Environment` `production`** existe (creado 2026-08-19T04:24:45Z), con política de rama restringida a `main`, sin reviewers obligatorios ni wait timer configurados. El reusable `_backend-deploy-cloud-run-production.yml` **está implementado e integrado a `main`** (PR #68, merge `7a5ec01d2dd99b5cef2188ee9e937ff633c9d342`, ver fila `T-F1.2` arriba para el detalle completo) con la secuencia completa de despliegue seguro, pero permanece con **0 ejecuciones y sin ningún llamador** (`production-deploy.yml` sigue siendo un bootstrap inerte, `workflow_dispatch` únicamente, sin `uses:` hacia ningún reusable). **Ningún servicio de Cloud Run de Production existe todavía** (`Cloud Run services list` = vacío). **Estado del esquema de la base de datos: no verificado** — sin conectividad de red desde este entorno hacia la IP privada de Cloud SQL, no se puede confirmar si las migraciones ya se aplicaron. `T-F1.1` está cerrada con Google Cloud Run seleccionado para Production; `T-F1.2` (pipeline de CD) está **EN PROGRESO** — ver fila `T-F1.2` arriba. |
| Tareas Git Windows #1-#3 (protección/preservación local) | **CERRADAS** | Auditoría de solo lectura del Git local de Windows, clasificación y preservación remota de todo el contenido local único identificado, y confirmación final de respaldo — completadas 2026-08-16. Contenido preservado en `backup/local-pre-limpieza-2026-07-21` y `backup/windows-local-wip-20260816`. **"Respaldado" no significa "aprobado para integrarse a `main`"** — ambas ramas son referencias de preservación, no cambios funcionales revisados ni aceptados. |
| Korixa Night Agent (v1, 5/5 mejoras de seguridad) | **CERRADO — 5/5** | PR #64 fusionado a `main` (`2a58551318a37ccbe4909d7e6c649f6ad6525324`) + PR #65 (puente Slack best-effort, merge `de172f19ef53e89e7f5e311461d55be7c92ad2e6`). Suite completa en verde en `main` (1109/1109). No es trabajo pendiente del roadmap de producto/infraestructura — **no reabrir** sin `REOPEN_REASON`/`NEW_EVIDENCE`/`LAST_CLOSING_EVIDENCE`/`WHY_CLOSURE_IS_INVALID` explícitos. |
| Night Agent hardening / programa 7 puntos | **CERRADA — 7/7 PASS** | Tareas 1-6 implementadas y fusionadas a `main` (PR #73 Tarea 1, #75 Tarea 2, #77 Tarea 3, #78 Tarea 4, #79 Tarea 5, #80 Tarea 6). Tarea 7 (primera operación real simultánea) tuvo dos intentos: el **Intento 1** (PR #81, sin fusionar, permanece como evidencia histórica de aceptación fallida) expuso un defecto real del sistema (`task-orchestrator-pr-number-unrecordable-post-creation`); **PR #82** lo corrigió (`recordPrOpened`/`BIND_PR_IDENTITY`); el **Intento 2** (PR #83) repitió la aceptación operativa real desde cero y la pasó íntegramente (B PASS, C PASS, P0=P1=P2=P3=0, defecto de sistema=falso). `main` = `0b971cc46c49bb3335cef438c503baa422659816`; CI post-merge run `32694668074` = 4/4 `SUCCESS`. Limpieza operativa posterior: `NIGHT_AGENT_OPERATIONAL_CLOSURE=PASS`, `STATE=CLOSED_CLEAN` (slot/lock del repositorio liberados, cero locks fantasma, evidencia histórica del Intento 1 y del hotfix verificada sin alteración). Ver historial 2026-08-24 para la cronología completa. |
| Gobernanza de `main` (checks obligatorios) | **4/4 checks requeridos, protección estricta activa** | `required_status_checks` = `Flutter — analyze + test`, `Firestore — reglas de seguridad (A3/A5)`, `Backend — migración + e2e (C2)`, `Night Agent — security + test`; `strict=true`; `enforce_admins=true`. |

**FUENTE_DE_VERDAD** (en caso de contradicción, en este orden):
1. `origin/main` actual y el estado real verificable (código, configuración, runtime).
2. Esta tabla — ESTADO VIGENTE.
3. Evidencia posterior de PR/CI/runtime documentada en la sección 5 (Historial).
4. `BACKLOG_MAESTRO.md` / `MASTER_EXECUTION_PLAN.md` para IDs, dependencias y criterios de aceptación (documentos congelados, no se editan).
5. Auditorías históricas (`docs/audits/AUDITORIA_FINAL/*`) únicamente como fotografías válidas de su propia fecha.

**REGLA_DE_REAPERTURA:** una tarea marcada **CERRADA** aquí no se reabre por encontrar una frase histórica aislada que la contradiga. Toda reapertura debe declarar explícitamente:
`REOPEN_REASON` / `NEW_EVIDENCE` / `LAST_CLOSING_EVIDENCE` / `WHY_CLOSURE_IS_INVALID`. Sin los cuatro campos justificados con evidencia real, no se reabre. (Mismo mecanismo ya vigente en "Cómo actualizar este documento", al final de este archivo.)

**REGLA_DE_RAMAS:** toda nueva implementación debe comenzar desde el HEAD vigente de `origin/main`, en una rama propia y aislada, salvo una excepción documentada y explícitamente autorizada. No reutilizar ramas antiguas de bloques ya cerrados.

**REGLA_DE_GIT_WINDOWS:** `backup/local-pre-limpieza-2026-07-21` y `backup/windows-local-wip-20260816` son referencias de preservación de contenido local histórico de una máquina Windows. No significan aprobación funcional, no se fusionan automáticamente a `main`, y no se eliminan automáticamente — cualquier decisión sobre su destino final queda pendiente del propietario.

### Próximo camino operativo

Orden vigente, sin declarar ninguno de estos pasos como ya ejecutado:

1. La infraestructura real de Production ya provisionada (Cloud SQL, Redis, VPC, Secret Manager, identidades WIF/IAM, GitHub Environment `production`) está documentada — ver fila "Production backend".
2. El reusable `_backend-deploy-cloud-run-production.yml` fue **implementado** (PR #68), reconciliado documentalmente (PR #69), y sus dos incógnitas de semántica de `gcloud` se resolvieron con evidencia real de laboratorio no-Production — **TEST A** (`INVOCATION_FLAG_OMISSION = RESOLVED_PROVEN`) y **TEST B** (`SAME_ATTEMPT_TAG_RETRY = RESOLVED_PROVEN_SAFE`), cada uno con revisión independiente propia. Esa misma evidencia descubrió 2 hallazgos P1 reales (límite combinado tag+servicio; audiencia de identity token incorrecta) más un hallazgo relacionado en el health check de rollback — todos corregidos y **fusionados a `main`** en **PR #70** (head `d79b51794ef05e03c658b9784267841a23798be8`, merge `e5158cd5825fde4b8bd5f154acf96d34b7d8161d`, reauditoría independiente `PASS_WITH_FINDINGS` con solo 1 P3 no bloqueante, CI post-merge 4/4 `PASS`, run `32588729509`). El reusable permanece **inalcanzable**: `production-deploy.yml` no lo invoca, cero referencias `uses:` en el repositorio, cero triggers `workflow_run`. Ninguno de estos bloques (TEST A, TEST B, PR #70) ejecutó ningún deploy ni mutó Production.
3. Próximo trabajo técnico, aún no ejecutado:
   a. Migraciones de Production: flujo de ejecución real, identidad dedicada, rol de base de datos de mínimo privilegio, orden de ejecución relativo a la creación de la candidata, verificación técnica (no solo declarativa) del esquema, estrategia expand/contract y forward-fix vs. rollback.
   b. Llamador/procedencia: guarda de frescura contra el `main` vigente para un futuro llamador (este reusable prueba `checkout == source_sha`, nunca que `source_sha` sea el `main` actual).
   c. Decisiones explícitas de producto/runtime del propietario: nombre del servicio, CPU, memoria, min/max instancias, `cors_allowed_origins`, política de invocación pública/IAM (la semántica de omitirla ya está resuelta; la decisión de producto en sí sigue pendiente), valor final de `vpc_egress_mode` (mecanismo Direct VPC Egress ya implementado; falta elegir `private-ranges-only` vs. `all-traffic`).
   d. Gobernanza y seguridad de datos: reviewer obligatorio en el GitHub Environment `production`; política de backups automáticos de Cloud SQL de Production, hoy deshabilitados.
   e. Diseño de un bloque futuro, separado y explícitamente autorizado, para la promoción a tráfico real de un primer despliegue (la creación/validación de salud de la candidata ya está implementada; la promoción automática está intencionalmente ausente por diseño).
   Solo después de (a)-(e) se autoriza, por separado, activar el llamador (`production-deploy.yml`) y, más adelante y también por separado, el primer deploy real.
4. Completar los bloques restantes de `T-F1.2` (activación del llamador, primer deploy real a Cloud Run, y eventualmente automatización de extremo a extremo) antes de poder declarar su cierre — el criterio de aceptación oficial ("un push a `main` despliega automáticamente al entorno correspondiente sin pasos manuales") todavía no está probado.
5. `T-F1.3` (staging real) y `T-F1.4` (extender CI) permanecen **NO INICIADAS**.
6. Staging continúa no iniciado.
7. Production backend: Artifact Registry, Cloud SQL, Redis, VPC, Secret Manager e identidades WIF/IAM ya provisionados y verificados en vivo; mecanismo de egress de red hacia la VPC ya decidido e implementado (Direct VPC Egress); Cloud Run de Production continúa sin desplegarse — ver fila "Production backend" para el detalle completo, incluida la advertencia de backups de Cloud SQL deshabilitados (riesgo de endurecimiento pre-tráfico, todavía sin decisión ni corrección).

`T-F0.1`, `T-F0.2`, `T-F0.3`, `T-F0.4`, `T-F0.5`, `QG0`, `T-F1.1` y el workstream de Korixa Night Agent (5/5) **no** aparecen en esta lista de pendientes — todos están cerrados/certificados (ver tabla arriba). `T-F1.2` está **en progreso**, no pendiente de iniciar.

---

## 1. Resumen del proyecto / Estado actual

RidePro publicó su primera línea base estable: **v0.5.0** — tag anotado y GitHub Release oficial sobre el commit `2e8cf132f0e8aa7219803c4879b1f90e2c188dd3` de `origin/main`. v0.5.0 integra, mediante 5 bloques funcionales secuenciales y sus respectivos PRs (merge commit tradicional en cada uno, hashes originales preservados), el trabajo ya construido en `feature/d2`:

- **Bloque 1** (PR #1) — Backend NestJS: módulo Workouts.
- **Bloque 2** (PR #2) — Estabilización Flutter/CI (Flutter 3.32.0 / Dart 3.8.0) + cliente de sesión de backend + feature Workouts en Flutter.
- **Bloque 3** (PR #3) — Imagen Docker de producción del backend.
- **Bloque 4** (PR #4) — Puente de autenticación Firebase↔NestJS.
- **Bloque 5** (PR #5) — Corrección de self-deadlock del pool de PostgreSQL + rate limiting híbrido.

Además: PR #6 (`CHANGELOG.md` de v0.5.0) y PR #7 (documentación de gobernanza — protocolo, ADRs, auditoría histórica, diseño de T-F0.1) también mergeados a `main`.

**Esto fue un proceso de integración/release de código ya construido y auditado en `feature/d2`, no la ejecución de nuevas tareas del backlog formal de 35 unidades (`docs/audits/AUDITORIA_FINAL/BACKLOG_MAESTRO.md`; ver nota de recuento corregido en la sección 2)** — ese backlog sigue vigente como está documentado, salvo los dos puntos siguientes, verificados con evidencia directa de esta integración:

- **`A1` (puente de autenticación Firebase↔NestJS) — CERRADO.** Código en producción real (`main`), probado end-to-end (suite e2e del backend, 86/86 verde en la validación final del Bloque 5).
- **`C2` (backend sin desplegar en ningún entorno real) — PARCIALMENTE CERRADO.** Existe una imagen Docker de producción validada (multi-stage, usuario no-root, `HEALTHCHECK`, sin código fuente TypeScript ni `devDependencies` en el runtime — Bloque 3). El **despliegue real a un hosting en vivo sigue sin ejecutarse** (`T-F1.1`, sin cambios respecto a la decisión D6 de la Parte B de separación de Firebase, `docs/audits/AUDITORIA_FINAL/15_PLAN_PARTE_B_SEPARACION_ENTORNOS_FIREBASE.md`).

Calidad de código consolidada: **B+** (sin nueva auditoría completa desde la oficial v1.1 — sin motivo para esperar cambio, ya que la integración trasladó código ya evaluado a `main`, no reescribió lógica).

### 1.1 Bloques posteriores a v0.5.0 (post-release, PRs #8-#12 y #14-#17 fusionados a `main`)

Tras v0.5.0, se ejecutó una segunda serie de bloques aislados, cada uno partiendo de `origin/main` en su propia rama, siguiendo el mismo patrón de la integración original. Se fusionaron a `main` (merge commit tradicional) los PRs **#8, #9, #10, #11, #12, #14, #15, #16 y #17**. **PR #13** (`ci(ios): add macOS build validation`) se cerró **sin fusionar** — fue un workflow experimental para diagnosticar un fallo de CocoaPods (deployment target insuficiente); una vez identificada la causa raíz, se cerró y se reemplazó por el PR estable **#14**, que aplica la corrección real (`IPHONEOS_DEPLOYMENT_TARGET` a iOS 14) junto con una versión limpia del workflow de validación.

- **PR #8** — `PROJECT_STATUS.md` reescrito reflejando el estado real posterior a v0.5.0 (corrigió afirmaciones obsoletas de la versión anterior del documento).
- **PR #9** — `T-F0.1` (crash de Wearables en Flutter Web) integrada a `main`: guarda de plataforma (`kIsWeb`) en `HealthPlatformGatewayImpl.checkAvailability()`, con test de regresión nuevo.
- **PR #10** — cobertura de tests de widgets para Login/Register/ForgotPassword (Auth) integrada a `main`.
- **PR #11** — scaffold base de Xcode para iOS (regenerado desde cero con Flutter 3.32.0, no copiado del WIP de `feature/d2`).
- **PR #12** — configuración de cliente Firebase iOS integrada (`GoogleService-Info.plist`, `firebase_options.dart`, Bundle ID `com.ridepro.app`, App ID real en `ridepro-dbafe`), excluyendo deliberadamente los cambios de `T-F0.2` que venían mezclados en el WIP original.
- **PR #14** — `IPHONEOS_DEPLOYMENT_TARGET` elevado a iOS 14 (requerido por una dependencia nativa) + workflow estable de validación de compilación iOS sin firma en runner macOS.
- **PR #15** — workflow experimental de smoke test en simulador iOS: compila para simulador, instala, lanza la app y confirma ausencia de crash inmediato (con captura de screenshot como evidencia adicional).
- **PR #17** — corrección: `GoogleService-Info.plist` (añadido en PR #12) no estaba referenciado en `ios/Runner.xcodeproj/project.pbxproj`, por lo que no se empaquetaba dentro de `Runner.app`. Se añadió su `PBXFileReference`/`PBXBuildFile`/entrada en `PBXResourcesBuildPhase` del target `Runner`.
- **PR #16** — extensión del smoke test de simulador con captura y clasificación de logs de inicialización de Firebase en runtime (`FIREBASE_RUNTIME_STATUS`). Actualizado con el fix de PR #17 y **fusionado después de #17** — su propio run de CI confirmó `FIREBASE_RUNTIME_STATUS=initialized` con `GoogleService-Info.plist` ya presente dentro de `Runner.app`.

**Estado de iOS al cierre de este bloque:** la app compila para dispositivo (sin firma) y para simulador, se instala, arranca, renderiza su pantalla de bienvenida sin crash inmediato, y Firebase se inicializa correctamente en runtime en un simulador real de GitHub Actions (conexión de red confirmada a `firebaseinstallations.googleapis.com` y `firebase-settings.crashlytics.com`, señales de arranque de `FirebaseAnalytics`/`FirebaseMessaging` presentes, sin el patrón de error "no se pudo localizar `GoogleService-Info.plist`"). Esto **no constituye** validación de Auth/Firestore/Analytics funcional real, ni de HealthKit, ni de APNs — sigue pendiente.

### 1.2 PRs #18 y #19 — sincronización documental y de configuración Firebase (post-PR #17)

- **PR #18** (`docs/sync-korixa-project-state`) — reescritura de este documento y de `README.md` reflejando el estado real de `main` en `cb6f65a` (ver la entrada correspondiente del 2026-07-31 en la Sección 5); no introdujo cambios de infraestructura.
- **PR #19** (`fix/tf02-sync-firestore-storage-config`) — integrado a `main` en el commit `253f2ce57461c70b6bdadb48b7ed517512c890be`: corrigió `firestore.indexes.json` a cero índices, agregó `storage.rules` con denegación por defecto (`allow read, write: if false` para todos los paths) y enlazó ese archivo desde `firebase.json`. **Estas reglas quedaron en el repositorio, no desplegadas contra ningún proyecto Firebase real** — ver `docs/audits/AUDITORIA_FINAL/17_CIERRE_FIRESTORE_RIDEPRO_DEVELOPMENT.md` sección 11 para la revalidación completa de solo lectura ejecutada después de este PR.

### 1.3 T-F0.2 Bloque 1 — PR #22, bootstrap de entornos Development/Production

- **PR #22** (`feat/tf02-environment-bootstrap-20260802`) — integrado a `main` (merge commit `99504dbb2719dc1d26a516132b9eaf2489b76e38`): `AppEnvironment` como única fuente de verdad de configuración por entorno, `bootstrapRideProApp` compartido entre `main.dart`/`main_development.dart` (nuevo entry point de Development), `resolveGoogleSignInClientId`/`resolveBackendBaseUrl` explícitos por entorno, `allowsDevBackendTestUser` (corrige un incidente real: el login de prueba del backend quedaba inalcanzable en cualquier build no-debug), `ApiConfig` eliminado. En ese PR, `environment_development.dart.backendBaseUrl` todavía usaba el mismo default local platform-aware que antes usaba `ApiConfig` — **no** apuntaba a ningún backend real. Reconciliado con WIP no commiteado hallado en `feature/d2` (nunca modificado, solo leído); dos URLs hardcodeadas en ese WIP se descartaron por no coincidir con la infraestructura real verificada.

### 1.4 T-F0.2 Bloque 2 — PR #23, conexión de Development al backend real

- **Corrección respecto a la versión anterior de este documento:** el Bloque 2 se describía como "implementado y validado... pendiente de push, PR y fusión" — **ya está fusionado.** **PR #23** (`feat/tf02-dev-backend-wiring-20260802`) integrado a `main` (merge commit `078dc52df4a36be17d66f844df64427c0d9f6bd7`): `environment_development.dart.backendBaseUrl` conectado a la URL real y ya validada del backend de Development (`https://ridepro-backend-dev-hmsnc2l3pq-rj.a.run.app/v1`); `productionEnvironment` sin cambios. Validado con `flutter analyze --fatal-infos` (0 issues), `flutter test` (254/254), ambos `flutter build web` exitosos, y verificación de aislamiento de bundle en ambas direcciones. Ningún recurso de infraestructura fue creado ni modificado — el backend ya existía desde el 2026-07-27/28.

### 1.5 T-F0.2 Bloque 5A — Android Development Foundation

Antes de este bloque, dos auditorías de solo lectura (Bloque 5A, diseño Android Development; Bloque 5A.0, origen del `applicationId` de Android Production) determinaron: (1) `com.ridepro.app.dev` es el package name seguro para Development, decidido de forma absoluta (no `applicationIdSuffix`) porque `defaultConfig.applicationId` de Production sigue siendo un placeholder sin resolver (`com.ridepro.app.YOUR_APPLICATION_ID`) — hallazgo nuevo, con línea de tiempo completa en `docs/audits/AUDITORIA_FINAL/24_ANDROID_DEVELOPMENT_FOUNDATION.md`; (2) ese placeholder es accidental (nació como scaffold el 2026-07-21, se conectó a Firebase real el 2026-07-23 sin corregirse), tiene decisión de reemplazo ya aprobada (Documento 15, D3, 2026-07-25: `com.ridepro.app`) pero nunca ejecutada, y su corrección queda **fuera de este bloque**, propuesta como Bloque 5C independiente.

Implementado en este bloque, rama `feat/tf02-android-dev-foundation-20260803` (worktree aislado desde `origin/main` en `e6d0a5f`): app Android nueva registrada en Firebase (`ridepro-development`, package `com.ridepro.app.dev`, nombre visible "Korixa Android Development"); flavor Gradle `development` en `android/app/build.gradle.kts` con `applicationId` absoluto (`defaultConfig`/flavor `production` sin cambios); `google-services.json` propio en `android/app/src/development/`; `firebase_options_development.dart` con soporte Android real (Web sin cambios; iOS explícitamente pendiente); `firebase.json` con la referencia de Development añadida sin alterar la de Production. Ejecutar siempre con `flutter build apk --flavor development -t lib/main_development.dart`.

**Validación:** `flutter analyze --fatal-infos` — mismos 12 issues preexistentes que en `origin/main` sin tocar (deprecación de `Radio` en `settings_page.dart`, deriva del SDK de Flutter instalado, no de este bloque), cero issues nuevos. `flutter test` — 248 passed / 6 failed, idéntico al baseline de `origin/main` sin tocar, mismos 6 tests fallando por nombre, ninguno relacionado con este bloque. `flutter build apk` en modo `--release` para ambos flavors (`development`/`production`) exitoso — `applicationId` compilado verificado exacto (`com.ridepro.app.dev` / placeholder sin cambios); auditoría de contenido del APK release en ambas direcciones: cero referencias cruzadas de proyecto, App ID o package name entre Development y Production. (El primer intento de build en modo `--debug` mostró referencias cruzadas dentro de `kernel_blob.bin` — confirmado como artefacto esperado del snapshot JIT no optimizado por tamaño de depuración, no una fuga real; el build `--release`, que sí aplica tree-shaking por punto de entrada, es la prueba válida de aislamiento). Google Sign-In Android en Development **no está validado** — pendiente de registrar SHA-1/SHA-256, explícitamente no declarado como cumplido.

**Incidencias de entorno de esta máquina, no del código:** una política de Control de Aplicaciones de Windows bloqueó inicialmente `impellerc.exe` (compilador de shaders del motor Flutter), reproducido de forma idéntica contra `origin/main` sin tocar — resuelto por el propietario activando Smart App Control y reiniciando. Un build `--release` posterior falló por longitud de ruta de Windows (contenía > 260 caracteres, por la ubicación del worktree aislado) — resuelto compilando una copia de los archivos fuente en una ruta más corta, sin mover la rama ni el worktree real.

### 1.6 T-F0.2 Bloque 6 — alias nombrados en `.firebaserc` (Puerta B)

Agrega `development` → `ridepro-development` y `production` → `ridepro-dbafe` a `.firebaserc`; el alias `default` (`demo-ridepro-security-tests`, un `projectId` ficticio usado únicamente por la suite de reglas de Firestore contra el emulador) queda exactamente igual. Auditoría previa confirmó que ningún workflow de CI ni script del repositorio depende de la resolución de `.firebaserc` — el único consumidor real (`firebase/rules-tests/package.json`) pasa `--project=demo-ridepro-security-tests` de forma explícita a `firebase emulators:exec`, sin usar `firebase use` ni leer el alias `default`.

**Validación:** `.firebaserc` sintácticamente válido; `firebase use development` resuelve `ridepro-development`; `firebase use production` resuelve `ridepro-dbafe`; `firebase use default` falla — comportamiento **preexistente**, confirmado idéntico contra una copia del `.firebaserc` original sin tocar (el `projectId` ficticio nunca fue resoluble vía `firebase use` interactivo, solo vía el flag `--project=` que usa el emulador). `git diff --check` limpio; diff funcional limitado a `.firebaserc`. Cierra la Puerta B del Documento 15 §12 para Development y Production (Staging permanece "no iniciado").

## 2. Porcentaje de avance

El backlog formal (Fase 0-3 + 6 epics de Fase 4 + 8 transversales) **no se recalculó exhaustivamente** en este cierre — la integración de v0.5.0 no ejecutó esas tareas bajo el protocolo de 10 etapas de `RIDEPRO_DEVELOPMENT_PROTOCOL.md`, solo consolidó en `main` código ya evaluado en la auditoría oficial.

**Total formal vigente: 35 tareas** — recuento directo de los encabezados `T-F*.*`/`T-TRANS.*` de `docs/audits/AUDITORIA_FINAL/BACKLOG_MAESTRO.md`: Fase 0 = 5, Fase 1 = 5, Fase 2 = 7, Fase 3 = 4, Fase 4 = 6, Transversales = 8 (5+5+7+4+6+8 = 35). El propio `BACKLOG_MAESTRO.md` conserva en su sección de cierre una suma histórica de 34 ("20 tareas de Fase 0-3 + 6 epics + 8 transversales") que subcuenta una unidad — el recuento directo de Fase 0-3 da 21, no 20. `BACKLOG_MAESTRO.md` no se modifica por ser fuente histórica congelada; este documento refleja el total corregido por recuento directo.

Único cambio verificado con evidencia directa:

| Ítem | Severidad | Estado anterior | Estado actual |
|---|---|---|---|
| `A1` | Alto | Abierto | **Cerrado** — código en `main`, suite e2e verde |
| `C2` | Crítico | Abierto | **Parcialmente cerrado** — artefacto de despliegue listo (imagen Docker), hosting real pendiente |
| Resto del backlog formal | — | Sin cambios | El backlog formal contiene 35 tareas según recuento directo (ver nota arriba). Este cierre no recalcula un porcentaje global ni un numerador de tareas cerradas sin revalidar individualmente cada unidad — ver sección 4. |

*(Se evita publicar un nuevo porcentaje global fabricado sin revalidar cada unidad individualmente. Ver "Próximos bloques" para el trabajo que reabrirá esta sección con evidencia nueva.)*

## 3. Estado de infraestructura

### Backend
NestJS + PostgreSQL. Módulos Equipment y Workouts completos y probados. Puente de autenticación Firebase↔NestJS operativo en `main` (Bloque 4). Corrección de self-deadlock del pool de conexiones bajo concurrencia aplicada y validada (Bloque 5). Suite e2e: 86/86 verde en la validación final pre-release.

**Corrección respecto a versiones anteriores de este documento:** la afirmación "despliegue real a un hosting en vivo: pendiente" era correcta únicamente para **Producción**, pero omitía que **el backend de Development ya está desplegado** desde el 2026-07-27/28: Cloud Run `ridepro-backend-dev` (región `southamerica-east1`, revisión `ridepro-backend-dev-00007-llf`, `Ready=True`) + Cloud SQL `ridepro-backend-dev-pg` (`POSTGRES_16`, `RUNNABLE`), dentro del proyecto GCP `ridepro-development`. Validado con pruebas de concurrencia reales (corrección de race condition en el intercambio Firebase↔NestJS, capacidad de pool documentada, rate limiting híbrido) — ver `docs/audits/AUDITORIA_FINAL/fase_4_1` y `fase_4_2`, ya en `main`. `CORS_ALLOWED_ORIGINS` del servicio ya está configurado para los dominios de Hosting Preview de Development. **T-F0.2 Bloque 2** conectó `environment_development.dart.backendBaseUrl` a esta URL real (`https://ridepro-backend-dev-hmsnc2l3pq-rj.a.run.app/v1`) — `productionEnvironment` no se tocó, sigue con el mismo default local que antes.

**Estado vigente de Producción (`T-F1.2`, actualizado en esta reconciliación):** `T-F1.1` está **cerrada** — Google Cloud Run seleccionado como hosting (2026-08-17). `T-F1.2` (pipeline de CD) está **EN PROGRESO**: el Artifact Registry de Production (`southamerica-east1-docker.pkg.dev/ridepro-dbafe/korixa-backend/api`) ya está **operativo para build/publish**, probado con una ejecución real y exitosa (`build-publish`, run `32210700072`, imagen Docker real publicada vía WIF). El reusable `_backend-deploy-cloud-run-production.yml` ya está **implementado e integrado a `main`** (PR #68, merge `7a5ec01d2dd99b5cef2188ee9e937ff633c9d342`) pero permanece **inalcanzable** (cero llamadores) — el despliegue real a **Cloud Run de Producción** todavía **no se ha ejecutado** — ver fila `T-F1.2` en la sección 0 para el detalle completo por bloque (`D1E1`/`D1F1`/`D2B1`/`D2B2`/`D2B3`/`D3B0`).

### Flutter
CI estabilizado en Flutter 3.32.0 / Dart 3.8.0 (Bloque 0/2). Feature Workouts completa (catálogo público + entrenamientos propios del usuario). Cliente de sesión de backend (`backend_auth_service`, `backend_dio_client`) integrado. `flutter analyze` 0 issues; suite de tests verde en cada uno de los 5 bloques.

### Docker
Imagen de producción del backend validada (Bloque 3): build multi-stage, usuario no-root, `HEALTHCHECK` contra `/v1/health`, sin código fuente TypeScript ni `devDependencies` en el runtime.

### Firebase
`ridepro-dbafe` sigue siendo el único proyecto de producción real (Auth, Android, Web, Windows-placeholder). La configuración de cliente iOS (Bundle ID `com.ridepro.app`, App ID, `firebase_options.dart`, `GoogleService-Info.plist`) ya está integrada en `main` (PRs #12/#17) y **el plist ya se empaqueta dentro de `Runner.app`**, con inicialización de Firebase confirmada en runtime en simulador (PR #16 — ver sección 1.1).

**`ridepro-development` — estado vivo, última actualización 2026-08-02 (detalle completo en `docs/audits/AUDITORIA_FINAL/17_CIERRE_FIRESTORE_RIDEPRO_DEVELOPMENT.md` secciones 11-12):**
- El proyecto **existe**; Firestore está creado en la región `southamerica-east1`.
- `storage.rules` (deny-by-default, `allow read, write: if false;`) **desplegadas y verificadas en producción**: prueba de lectura anónima real sobre el bucket → `403`. Despliegue ejecutado con `firebase deploy --only storage --project ridepro-development --non-interactive`, sin tocar ningún otro servicio.
- **Bucket de Cloud Storage `ridepro-development.firebasestorage.app` existe**, región `SOUTHAMERICA-EAST1`, clase Standard (`REGIONAL` en la nomenclatura heredada de la API), vacío (0 objetos) — creado manualmente y de forma autorizada por el propietario desde Firebase Console el 2026-08-02, después de la fusión del PR #20 (que documentaba correctamente cero buckets en ese momento anterior).
- Facturación **vinculada y con cuenta de Cloud Billing abierta**, con un presupuesto de alertas configurado ("presupuesto mensual korixa", alcance `ridepro-development`/todos los servicios, umbrales 50%/90%/100% — no es un límite automático de gasto). Etiqueta comercial exacta **"Blaze" no confirmada visualmente** (sin evidencia explícita de esa palabra en Console); confirmación previa era solo técnica, por vinculación de Cloud Billing.
- Authentication — verificación visual del propietario en Console: **Email/Password, Google y Apple habilitados**; Teléfono y Anónimo no figuraban habilitados en esa verificación visual; MFA por SMS no figuraba habilitado. "Apple habilitado en Firebase" **no confirma** que Apple Developer esté configurado completamente. No se modificó IAM ni ningún proveedor en estos subbloques.
- Existe **una app Web registrada**, con nombre visible heredado `RidePro Web (Development)` — registro autorizado por el propietario con posterioridad al plan inicial de esta sub-fase, no una anomalía.
- **T-F0.2 Bloque 5A** agregó una **app Android real** (`com.ridepro.app.dev`, "Korixa Android Development") — 2 apps registradas en total en `ridepro-development` (Web + Android). Google Sign-In Android pendiente de SHA-1/SHA-256.
- La separación completa de entornos (`T-F0.2`/`C1`: matriz Staging/Production) **sigue sin completarse** — ver sección 4 para el detalle de lo pendiente. **Corrección de esta reconciliación:** la mención histórica de "trabajo adicional aún no integrado desde `feature/d2`" ya no aplica — verificado en vivo que `feature/d2` es ancestro completo de `main` (0 commits propios no incluidos), sin trabajo pendiente de integrar.

**Hallazgo nuevo sobre Android Production (`ridepro-dbafe`), auditoría de solo lectura previa al Bloque 5A:** el `applicationId` de Android Production **nunca fue corregido** desde su placeholder original de scaffold — sigue siendo literalmente `com.ridepro.app.YOUR_APPLICATION_ID`, tanto en `android/app/build.gradle.kts` como en la app Android ya registrada en Firebase (`google-services.json` real, App ID confirmado contra `firebase apps:list --project ridepro-dbafe`). El Documento 15 (decisión D3, 2026-07-25) da por hecho que este valor "ya fijado" a `com.ridepro.app` — no es así; nunca se ejecutó. Compila, instala y usa Firebase/Google Sign-In con normalidad bajo el placeholder actual; no hay evidencia de distribución real (Play Store, dispositivos físicos, usuarios) en ninguna fuente auditada. Corregirlo requiere registrar una app Android *nueva* en Firebase (el `package_name` de una app existente es inmutable), por lo que queda fuera del alcance de Development — propuesto como Bloque 5C independiente, no autorizado todavía. Detalle completo en `docs/audits/AUDITORIA_FINAL/24_ANDROID_DEVELOPMENT_FOUNDATION.md`.

**Cloud Firestore en `ridepro-dbafe` (producción) — estado verificado, no una suposición:** `docs/audits/AUDITORIA_FINAL/17_CIERRE_FIRESTORE_RIDEPRO_DEVELOPMENT.md` (cronología, ítem 4) documenta un intento real de determinar la región de Firestore de producción para replicarla en `ridepro-development`, y ese intento **reveló que Cloud Firestore nunca fue habilitado en `ridepro-dbafe`** — hallazgo verificado con evidencia técnica directa (no una API de permisos fallando, sino ausencia de base de datos), documentado el 2026-07-25, sin ID de backlog todavía y sin decisión del propietario sobre cómo proceder. Este hallazgo no ha sido re-verificado desde entonces (podría haber cambiado si alguien lo habilitó manualmente después de esa fecha, fuera de este repositorio) — se cita aquí como el último estado confirmado con evidencia, no como un hecho re-comprobado en esta tarea.

### iOS
Compila para dispositivo (sin firma) y para simulador en runner macOS de GitHub Actions; se instala, arranca y renderiza su pantalla de bienvenida sin crash inmediato; Firebase se inicializa correctamente en runtime (evidencia de PR #16, ver sección 1.1). **No validado:** Auth/Firestore/Analytics funcional real, HealthKit, APNs, Apple Sign-In en dispositivo real, firma de código real.

### Windows
Directorio nativo `windows/` **no existe todavía** en `main` (`T-F2.7` sigue abierta en el backlog — "generar proyecto nativo + validar plugins de riesgo"). Es una plataforma objetivo declarada (ver `README.md` y `docs/product/PRODUCT_IDEAS_REGISTRY.md`), no una plataforma ya construida.

### CI
3 jobs base (`Flutter — analyze + test`, `Firestore — reglas de seguridad`, `Backend — migración + e2e`) verdes en cada PR desde el Bloque 0, más 2 workflows experimentales no obligatorios para iOS (`iOS — build validation`, `iOS — simulator smoke test`), verdes desde su integración (PRs #14/#15/#16). Alcance de OAuth de GitHub Actions ampliado (`workflow` scope) para permitir PRs que modifican `ci.yml`.

### Tests
Evidencia directa verificada en el run de CI del PR #16 (`30670032910`/`30670032915`, 2026-07-31): **Flutter — 205 pruebas aprobadas** ("🎉 205 tests passed."), **Firestore — 28 pruebas aprobadas** (reglas de seguridad contra emulador), **Backend — 86 pruebas aprobadas** ("Tests: 86 passed, 86 total") contra PostgreSQL real, sin `ECONNRESET` ni timeouts de pool.

## 4. Backlog abierto

*(Detalle completo, punto-en-el-tiempo, en `docs/audits/AUDITORIA_FINAL/` — no se duplica aquí para evitar que este documento y esos informes diverjan con el tiempo. `BACKLOG_MAESTRO.md` es una auditoría congelada y no se modifica; este documento solo refleja el estado de ejecución encima de él.)*

### Estado de Fase 0 (`T-F0.1`-`T-F0.5`)

- **`T-F0.1`/`A2`/`PLAT-1` (crash de Wearables en Flutter Web) — implementación integrada a `main` (PR #9), con evidencia automatizada aprobada** (`flutter analyze` 0 issues, test de regresión en verde). El protocolo (`RIDEPRO_DEVELOPMENT_PROTOCOL.md` §1, Etapa 5/Puerta 3) contempla además una validación manual en navegador real — esa comprobación adicional **sigue sin ejecutarse** por la misma limitación de infraestructura ya documentada (sin navegador real disponible en este entorno). Esto no significa que el código no esté integrado: el código y su test automatizado sí están en `main`; lo pendiente es exclusivamente esa verificación manual complementaria.
- **`T-F0.2`/`C1`** — separación completa de entornos Firebase (Development/Staging/Production): **abierta**. **La matriz formal de entornos exigida por el criterio de aceptación oficial de `C1` (`BACKLOG_MAESTRO.md`) ya existe** — `docs/audits/AUDITORIA_FINAL/23_MATRIZ_FORMAL_ENTORNOS.md`, fuente de verdad detallada de Development/Staging/Production (16 dimensiones), reemplaza como referencia el detalle que antes vivía disperso en esta sección. Resumen de su veredicto: Development tiene infraestructura base real y funcional (Firestore, Storage, Authentication, backend Cloud Run/Cloud SQL ya conectado en código — PRs #22/#23); Staging **no iniciado** (ningún proyecto GCP/Firebase existe); Production conserva únicamente su configuración de cliente histórica, sin Firestore/backend propios. Bajo el estándar de las 10 puertas obligatorias del Documento 15 §12, Development cumplía 4 de 10 al cierre del Bloque 3 (puertas B, E y H sin cumplir — ver Documento 23 §6). **T-F0.2 Bloque 5A** avanzó la Puerta E: Development ahora tiene Android real registrado (`com.ridepro.app.dev`) y build verificado (`--release`, aislamiento confirmado) — Google Sign-In Android queda explícitamente pendiente de SHA-1/SHA-256, por lo que la Puerta E pasa de "no cumplida" a **parcial**, no a cumplida. **T-F0.2 Bloque 6** cierra la Puerta B: `.firebaserc` ahora tiene alias nombrados `development`/`production` consistentes con `firebase_options_development.dart`/`firebase_options.dart`. Puerta H (CI/CD hacia Development) sigue sin cumplir — único pendiente restante de los 3 originales del Bloque 3. **`C1` sigue sin declararse cerrada**, aunque el criterio literal más simple de `BACKLOG_MAESTRO.md` (2 proyectos reales + matriz documentada) esté sustancialmente cubierto. Ver `docs/audits/AUDITORIA_FINAL/25_FIREBASERC_ALIASES.md` para el detalle de este bloque y el estado actualizado de los pendientes.

**Corrección (cierre 2026-08-15) — alcance limitado a Puerta H/`T-F0.2`/`C1`:** la frase "Puerta H... sigue sin cumplir" de este párrafo describía correctamente el estado en su propia fecha (2026-08-03) y no se reescribe. Queda superada por el cierre real documentado en el historial del 2026-08-15: primer deploy automatizado real a Cloud Run Development (Workload Identity Federation + build + push + deploy `--no-traffic` + validación de candidata + switch explícito de tráfico + health check), `workflow_dispatch` run `31924059938`, conclusión `SUCCESS`. Con esto, las 10 puertas del Documento 15 §12 quedan **Cumplida** para Development y `T-F0.2`/`C1` **se declara formalmente cerrada**. Ver historial 2026-08-15 para el detalle completo.

**Corrección (reconciliación 2026-08-13):** esta misma frase quedó desactualizada respecto a su propia entrada posterior — la evidencia runtime del 2026-08-09 (ver historial) valida un login de Google real y funcional en Android Development, no solo el registro de la app. La Puerta E se reclasifica **Cumplida**, no "parcial", desde esa entrada. Ver historial 2026-08-13 para la reconciliación completa de las 10 puertas.

**Actualización 2026-08-09 — PR #31, #33, #32, #34 (todas `MERGED`, verificadas contra `main` = `f1646958612dd9b0054d457c030509ab70741988`; orden real de merge: #31 → #33 → #32 → #34):** PR #31 (merge `1c4e3b3eeacb38d9b886a54785d2b2c3add1b407`, 2026-08-06) agregó a `android/app/src/development/google-services.json` el cliente OAuth del SHA-1 debug de Android Development (`com.ridepro.app.dev`, `ridepro-development`, `mobilesdk_app_id` `1:1020003121433:android:cb71425e96b4fe430f6d88`) ya registrado en Firebase. PR #33 (merge `05114fb36ff20c6fdc4541170c76ef43ce6dac91`, 2026-08-07) actualizó `.github/workflows/ios-simulator-smoke.yml` para detectar `CFBundleIdentifier` dinámicamente vía `PlistBuddy`, quedando compatible con el cambio de Bundle ID de iOS que se integró poco después mediante PR #32 (merge `4cca134dd16509fa48bdc511083595aa27a234ad`, mismo día), el cual renombra el Bundle ID de iOS Production a `com.korixa.app` (proyecto `ridepro-dbafe` sin cambios). PR #34 (merge `f1646958612dd9b0054d457c030509ab70741988`, 2026-08-08) registró iOS Development (`com.korixa.app.dev`, proyecto `ridepro-development`), con 6/6 checks verificados exitosos: 3 de `ci.yml` (Backend, Firestore, Flutter), 2 de `ios-build.yml` (Production y Development), 1 de `ios-simulator-smoke.yml`.

Como **evidencia runtime/local verificada durante la sesión de validación del 2026-08-09** (no proveniente de GitHub/CI): se validó Google Sign-In en Android Development — SHA-256 del certificado debug verificado localmente y confirmado ya registrado en Firebase junto al SHA-1 (App ID `1:1020003121433:android:cb71425e96b4fe430f6d88`); `flutter run --flavor development -t lib/main_development.dart` ejecutado en un emulador Android con Google Play (API 34); login real completado; usuario con provider `google.com` confirmado por consulta de solo lectura a Firebase Authentication de `ridepro-development` (datos sanitizados, sin correo/UID completo/tokens). Durante la validación se verificó mediante ADB que la actividad en primer plano era `com.ridepro.app.dev/com.ridepro.app.MainActivity`.

**La suite de reglas de Firestore de CI (`firestore-rules-tests`) sigue ejecutándose exclusivamente contra el emulador, no contra ningún proyecto real** — sin cambios respecto a lo ya documentado. `T-F0.2`/`C1` **sigue sin declararse cerrada**: no se afirma aquí el cierre formal de ninguna puerta del Documento 15 §12 más allá de lo explícitamente evidenciado en esta entrada.
- **`T-F0.3`** — `docker-compose.yml` para desarrollo local: **cerrada**, integrada a `main` mediante **PR #29** (merge commit `2d8e5751931c2a76a300dcaf7a7ee812ad19a2d0`).
- **`T-F0.4`** — rate limiter en memoria → Redis: **cerrada**, integrada a `main` mediante **PR #30** (merge commit `ce54cb5fd769177e32a4fa3feb8d144bee1d91b5`).
- **`T-F0.5`** — paginación real en `equipment`/`workouts`: **abierta**, sin iniciar.

### Resto del backlog

- **`M1`-`M10`, `B1`-`B10`** — sin cambios desde la auditoría oficial v1.1 (`docs/audits/AUDITORIA_FINAL/07_RIESGOS_TECNICOS.md`).
- Deuda técnica de la auditoría original (dependencias de codegen sin uso, tabla `ride_sessions` duplicada, `audit_log` sin escritura real, cobertura de accesibilidad mínima, entre otras) — sin cambios, ver `docs/audits/AUDITORIA_FINAL/BACKLOG_MAESTRO.md`.
- **Corrección de concurrencia en `EquipmentRepository.applyDefault()` (área `C2`, backend, sin relación con T-F0.2)** — hallazgo detectado incidentalmente durante la investigación de un fallo de CI en la PR #26 (T-F0.2 Bloque 6): un deadlock real de PostgreSQL (`deadlock detected`, SQLSTATE `40P01`) en el `SELECT ... FOR UPDATE` sin `ORDER BY` de `applyDefault()`, reproducible bajo concurrencia real (5+ requests marcando equipos distintos como default en la misma categoría). Corregido agregando `ORDER BY id` a esa consulta — un solo archivo, una línea. Ver `docs/audits/AUDITORIA_FINAL/26_EQUIPMENT_DEADLOCK_FIX.md` para el diagnóstico completo, la demostración formal, y la evidencia empírica (8/8 reproducciones del deadlock real sin el fix, 10/10 corridas limpias con el fix, bajo un escenario de estrés de 25 ítems concurrentes). Desarrollado en una rama completamente independiente de la PR #26, sin cherry-pick ni mezcla.

**Sin evidencia de despliegue productivo activo** (backend, Firebase separado por entornos, o release pública de Korixa) — el estado actual es de integración/validación técnica en `main`, no de operación en producción.

## Próximos bloques

Con `T-F0.1`, la cobertura de tests de Auth, la validación de plataforma iOS (scaffold + Firebase + bundling del plist + inicialización en runtime) ya integrados a `main` (PRs #9-#17), y `T-F0.2`/`C1` con su bootstrap, backend de Development, matriz formal (Documento 23), Android Development (Bloque 5A) y alias de `.firebaserc` (Bloque 6) ya integrados, además de `T-F0.3` (PR #29) y `T-F0.4` (PR #30) ya cerradas, el orden priorizado para el trabajo posterior es:

1. **`T-F0.2`/`C1` — CERRADA.** Las 10 puertas del Documento 15 §12 están **Cumplida** para Development (A, B, C, D, E, F, G, H, I, J — ver historial 2026-08-15 para el cierre de Puerta H). No queda ningún punto pendiente de este ítem. Trabajo futuro relacionado con Staging/Production (nunca iniciado) queda fuera de `C1` tal como estaba definido, y no reabre esta tarea — cualquier reapertura requeriría justificar explícitamente `REOPEN_REASON`/`NEW_EVIDENCE`/`LAST_CLOSING_EVIDENCE`/`WHY_CLOSURE_IS_INVALID` según la regla de la sección "Cómo actualizar este documento".
2. **Bloque 5C (propuesto, no autorizado) — corregir el `applicationId` placeholder de Android Production** (`com.ridepro.app.YOUR_APPLICATION_ID` → `com.ridepro.app`, decisión D3 ya aprobada mas nunca ejecutada): requiere registrar una app Android nueva en `ridepro-dbafe` (el `package_name` de la app actual es inmutable), nuevo SHA-1/SHA-256 de release, y verificar disponibilidad en Play Store antes de proceder — ver `docs/audits/AUDITORIA_FINAL/24_ANDROID_DEVELOPMENT_FOUNDATION.md` sección de auditoría 5A.0.
3. **`T-F0.5`** — paginación real y segura en `equipment`/`workouts`: sin iniciar, sin dependencias técnicas bloqueantes respecto al punto anterior.

Entre estos dos, ninguna prioridad documentada obliga un orden — `BACKLOG_MAESTRO.md` no los distingue por severidad relativa entre sí (Bloque 5C es una corrección de identidad de Android Production sin evidencia de distribución real; `T-F0.5` es deuda funcional del backend). Se listan en el orden ya establecido por bloques anteriores de este documento, sin asumir cuál debe ejecutarse primero — decisión pendiente del propietario.

**`c3d95bd` fue la base de `main` auditada al cerrar este bloque** — no se presenta como el HEAD vigente de forma permanente: todo trabajo futuro debe partir del **HEAD vigente de `origin/main`** después de cualquier PR posterior, en una rama propia, siguiendo el mismo patrón de bloques aislados ya usado en toda la integración hasta ahora.

## 5. Historial de cambios

| Fecha | Evento |
|---|---|
| 2026-07-24 | Auditoría Arquitectónica Oficial v1.0 completada (9 documentos + informe de soporte + resumen ejecutivo + índice). |
| 2026-07-24 | Revisión independiente v1.0 (`REVISION_FINAL_AUDITORIA.md`) — identificó defectos de citación/nomenclatura, ninguno técnico. |
| 2026-07-24 | Corrección documental v1.1 (`REVISION_FINAL_AUDITORIA_v1.1.md`) — 8 archivos corregidos, 0 hallazgos técnicos alterados. Auditoría aprobada como documentación oficial. |
| 2026-07-24 | `MASTER_EXECUTION_PLAN.md` y `BACKLOG_MAESTRO.md` generados — transformación de la auditoría en plan de ejecución (34 tareas, 0 riesgos nuevos). |
| 2026-07-24 | `RIDEPRO_DEVELOPMENT_PROTOCOL.md` emitido (v1.0) — estándar operativo permanente para todo desarrollo futuro. |
| 2026-07-24 | Creación de este documento (`PROJECT_STATUS.md`) — estado inicial ("día cero"), 0/34 tareas ejecutadas. |
| 2026-07-24 | `T-F0.1` implementada (`lib/core/health/health_platform_gateway_impl.dart`, guarda de plataforma Web) + test de regresión nuevo. `flutter analyze` (0 issues), `flutter test` (189/189 verde) y `flutter build web --release` confirmados. Validación manual en navegador real no completada por limitación de infraestructura del entorno — tarea en revisión final, no cerrada formalmente. Hallazgo nuevo `H-WEARABLES-NEW-1` documentado, no corregido. |
| 2026-07-24 | `T-F0.2`/`C1`: inventario completo + plan reversible de separación de Firebase por entornos, modo auditoría/solo lectura. Hallazgo: iOS lanza `UnsupportedError` en `Firebase.initializeApp()`. |
| 2026-07-24 | `T-F0.2`/`C1`, Fase 1 de prerrequisitos ejecutada parcialmente: `.gitignore` fortalecido — ✅ Aprobado. `storage.rules` creado — ✅ Aprobado. Estructura de proyecto Xcode de iOS generada por primera vez — ⚠ Requiere revisión, detenida a propósito antes de fijar Bundle ID. |
| 2026-07-24 | Firebase para iOS completado a nivel estático: Bundle ID `com.ridepro.app` confirmado, app iOS registrada en `ridepro-dbafe`, `firebase_options.dart`/`GoogleService-Info.plist`/`Info.plist` reales. Veredicto: aprobado con observaciones — build/instalación real pendiente en macOS. |
| 2026-07-25 | Fase 1 de Firebase (iOS) declarada oficialmente cerrada. Decisiones D1-D8 de la Parte B (separación de entornos) aprobadas por el propietario. |
| 2026-07-25 | Fase 1 de la Parte B iniciada — sub-fase Firestore de `ridepro-development` completada y aprobada. Hallazgo nuevo: Cloud Firestore nunca fue habilitado en `ridepro-dbafe`, documentado y no corregido. |
| 2026-07-30 | **v0.5.0 publicado.** Integración de `feature/d2` en `main` vía 5 bloques funcionales secuenciales (PRs #1-#5, merge commit tradicional, hashes originales preservados): Workouts backend+Flutter (Bloque 1), estabilización Flutter/CI + cliente de sesión de backend (Bloque 2), imagen Docker de producción (Bloque 3), puente de autenticación Firebase↔NestJS (Bloque 4), corrección de self-deadlock del pool de PostgreSQL (Bloque 5). `CHANGELOG.md` agregado (PR #6). Tag `v0.5.0-rc1` validado exhaustivamente (backend, Flutter, Firestore, Docker, smoke funcional) y promovido a tag estable `v0.5.0` sobre el mismo commit `2e8cf132f0e8aa7219803c4879b1f90e2c188dd3`, sin commits intermedios. GitHub Release oficial publicado. |
| 2026-07-30 | Documentación de gobernanza (protocolo, ADRs, auditoría histórica, diseño de T-F0.1) trasladada desde el WIP de `feature/d2` a `main` (PR #7) como registro histórico punto-en-el-tiempo, con la sanitización de un dato personal menor (correo del propietario reemplazado por una referencia neutral). Este documento (`PROJECT_STATUS.md`) fue excluido deliberadamente de ese PR por requerir esta reescritura propia. |
| 2026-07-30 | Este documento actualizado por primera vez desde su creación — refleja el cierre oficial de v0.5.0, corrige las afirmaciones ya falsas de la versión anterior (0% completado, backend sin desplegar, puente Firebase inexistente) y define el orden priorizado de los 4 próximos bloques de trabajo. |
| 2026-07-30 | `T-F0.1` integrada a `main` (PR #9): guarda de plataforma (`kIsWeb`) en `HealthPlatformGatewayImpl.checkAvailability()`, con test de regresión nuevo. |
| 2026-07-30 | Cobertura de tests de widgets de Auth (Login/Register/ForgotPassword) integrada a `main` (PR #10). |
| 2026-07-30 | Scaffold base de Xcode para iOS integrado a `main` (PR #11), regenerado desde cero con Flutter 3.32.0. |
| 2026-07-30 | Configuración de cliente Firebase iOS integrada a `main` (PR #12): `GoogleService-Info.plist`, `firebase_options.dart`, Bundle ID `com.ridepro.app`, App ID real en `ridepro-dbafe`; excluidos deliberadamente los cambios de `T-F0.2` mezclados en el WIP original. |
| 2026-07-31 | `IPHONEOS_DEPLOYMENT_TARGET` elevado a iOS 14 (PR #14) + workflow estable de validación de compilación iOS sin firma en runner macOS. |
| 2026-07-31 | Workflow experimental de smoke test en simulador iOS integrado a `main` (PR #15): build, boot, instalación, lanzamiento y verificación de ausencia de crash inmediato, con captura de screenshot como evidencia. |
| 2026-07-31 | Corrección: `GoogleService-Info.plist` no estaba referenciado en `ios/Runner.xcodeproj/project.pbxproj` y no se empaquetaba en `Runner.app` (PR #17) — añadidas las 4 entradas mínimas (`PBXFileReference`, `PBXBuildFile`, grupo `Runner`, `PBXResourcesBuildPhase` del target `Runner`). |
| 2026-07-31 | Workflow de smoke test extendido con captura y clasificación de logs de inicialización de Firebase en runtime (PR #16), actualizado con el fix de PR #17 y fusionado después de este. Run de CI propio del PR confirmó `FIREBASE_RUNTIME_STATUS=initialized`. |
| 2026-07-31 | Producto renombrado a **Korixa** a nivel de marca/documentación — sin migración de identificadores técnicos heredados (repositorio, Bundle ID, nombres de archivo). Este documento y `README.md` sincronizados con el estado real de `main` (`cb6f65a`); creado `docs/product/PRODUCT_IDEAS_REGISTRY.md` como registro vivo y permanente de ideas de producto, separado de `BACKLOG_MAESTRO.md` (que permanece congelado, sin modificar). |
| 2026-07-31 | Corrección del PR #18 (mismo commit de sincronización, sin abrir PR nuevo): (1) precisado que se fusionaron a `main` los PRs #8-#12 y #14-#17 — `PR #13` se cerró **sin fusionar** (workflow experimental de diagnóstico) y fue reemplazado por el PR estable `#14`; (2) corregida la contradicción sobre Cloud Firestore en `ridepro-dbafe`: el hallazgo de que nunca fue habilitado en producción está evidenciado en `docs/audits/AUDITORIA_FINAL/17_CIERRE_FIRESTORE_RIDEPRO_DEVELOPMENT.md` (intento real de determinar su región, 2026-07-25) — no re-verificado en esta tarea, citado como último estado confirmado; (3) `README.md` corregido para no afirmar hardware de marcas específicas (Wahoo/Tacx/Elite/etc.) como validado físicamente — se aclara "compatibilidad por estándar prevista, validación con hardware real pendiente"; (4) `docs/product/PRODUCT_IDEAS_REGISTRY.md` corregido: Korixa Receiver pasó de "Propuesta" a "Aprobada" con alcance detallado, se separó el offline de Firestore/Flutter del offline futuro de la estación, se diferenció la competencia local de gimnasio del multijugador masivo por Internet, y se agregaron 5 ideas omitidas (motor local de carrera, módulo visual Unity, salida a TV, rutas GPX, y la fila de offline de la estación) — total actualizado a 34 ideas, ninguna eliminada. |
| 2026-08-01 | **PR #19** (`fix/tf02-sync-firestore-storage-config`) integrado a `main` (merge commit `253f2ce57461c70b6bdadb48b7ed517512c890be`): `firestore.indexes.json` corregido a cero índices, `storage.rules` nuevo con denegación por defecto, enlazado desde `firebase.json`. Reglas presentes en el repositorio, no desplegadas contra ningún proyecto Firebase real en ese PR. |
| 2026-08-02 | Revalidación de solo lectura de `ridepro-development` posterior al PR #19 (`T-F0.2`/`C1`), sin ninguna mutación de infraestructura: confirmado `billingEnabled: true` con cuenta de Cloud Billing vinculada y abierta (técnicamente Blaze, sin inspección visual de la etiqueta en Console); presupuestos/alertas no verificables (API deshabilitada, no habilitada); cero buckets físicos de Storage; proveedores de Authentication no verificables con la sesión CLI actual (`403 Forbidden`); una app Web registrada (`RidePro Web (Development)`, excepción cronológica autorizada, pendiente renombrar a `Korixa Web (Development)`, no ejecutado). Detalle completo en `docs/audits/AUDITORIA_FINAL/17_CIERRE_FIRESTORE_RIDEPRO_DEVELOPMENT.md` sección 11. `T-F0.2`/`C1` permanece abierta con avance parcial, no declarada completada. |
| 2026-08-02 | Posterior al PR #20 (fusionado 17:50 UTC, cero buckets confirmados en ese momento), el propietario creó manualmente y de forma autorizada el bucket `ridepro-development.firebasestorage.app` desde Firebase Console (19:01 UTC). Con autorización explícita, se ejecutó un despliegue controlado limitado a `firebase deploy --only storage --project ridepro-development --non-interactive`: reglas deny-by-default publicadas y verificadas con una prueba de lectura anónima real (`403`); ningún otro servicio tocado. El propietario reportó además, por verificación visual propia en Console (no confirmada de forma independiente por esta sesión): presupuesto de alertas activo (50%/90%/100%, sin ser límite automático de gasto) y Authentication con Email/Password, Google y Apple habilitados (Teléfono y Anónimo no figuraban habilitados en esa verificación visual; Apple en Firebase no implica Apple Developer completo). `T-F0.2`/`C1` pasa a "abierta — infraestructura base completada; cierre formal pendiente de revisión de criterios" — no se declara su cierre total. Detalle completo en `docs/audits/AUDITORIA_FINAL/17_CIERRE_FIRESTORE_RIDEPRO_DEVELOPMENT.md` sección 12. |
| 2026-08-02 | **PR #22** (`feat/tf02-environment-bootstrap-20260802`, T-F0.2 Bloque 1) integrado a `main` (merge commit `99504dbb2719dc1d26a516132b9eaf2489b76e38`): `AppEnvironment`, `bootstrapRideProApp` compartido, `main_development.dart` nuevo, `resolveGoogleSignInClientId`/`resolveBackendBaseUrl` explícitos por entorno, `allowsDevBackendTestUser` (corrige un incidente real de login de prueba inalcanzable fuera de `kDebugMode`, con pruebas dedicadas), `ApiConfig` eliminado. `backendBaseUrl` de Development seguía en el default local, sin conectar a ningún backend real en ese PR. |
| 2026-08-02 | Auditoría de solo lectura previa al Bloque 2: se verificó, vía comandos de consulta (`gcloud run services list`, `gcloud sql instances list`, lectura de `docs/audits/AUDITORIA_FINAL/fase_4_1`/`fase_4_2`), que el backend de Development (Cloud Run `ridepro-backend-dev` + Cloud SQL `ridepro-backend-dev-pg`) ya estaba desplegado y validado desde el 2026-07-27/28 — hallazgo no reflejado hasta ahora en este documento. Se identificó además que `environment_development.dart.backendBaseUrl` (PR #22) seguía sin conectar a esa infraestructura real. |
| 2026-08-02 | **T-F0.2 Bloque 2** — `environment_development.dart.backendBaseUrl` conectado a la URL real y ya validada del backend de Development (`https://ridepro-backend-dev-hmsnc2l3pq-rj.a.run.app/v1`); `productionEnvironment` sin cambios. Implementado y validado en la rama `feat/tf02-dev-backend-wiring-20260802` (commit `c392e7790dbb786058645a44aaabfe642ffd589b`, padre `99504dbb2719dc1d26a516132b9eaf2489b76e38`) — **pendiente de push, PR y fusión**, no ejecutados en esta tarea. Validado con `flutter analyze --fatal-infos` (0 issues), `flutter test` (254/254), `flutter build web` para `main_development.dart` y `main.dart` (ambos exitosos), y verificación de aislamiento del bundle (Development contiene la URL autorizada y cero identificadores de Producción; Producción contiene cero referencias al backend de Development). Ningún recurso de infraestructura fue creado, modificado ni desplegado en este bloque — el backend ya existía. `T-F0.2`/`C1` sigue sin declararse cerrada — la matriz formal de entornos (criterio de aceptación oficial de `C1`) permanece pendiente. |
| 2026-08-02 | **PR #23** (`feat/tf02-dev-backend-wiring-20260802`, T-F0.2 Bloque 2) integrado a `main` (merge commit `078dc52df4a36be17d66f844df64427c0d9f6bd7`), cerrando el ciclo push/PR/checks/merge del bloque descrito en la entrada anterior — 5/5 checks en verde (`Flutter`, `Firestore`, `Backend`, `iOS — build`, `iOS — simulator smoke test`). |
| 2026-08-03 | **T-F0.2 Bloque 3** — matriz formal de entornos redactada: `docs/audits/AUDITORIA_FINAL/23_MATRIZ_FORMAL_ENTORNOS.md`, cubriendo las 16 dimensiones exigidas (nombre, proyecto, entry point, Firebase options, backend/URL, base de datos, Firestore, Storage, Authentication, Hosting, CORS, logs, secretos, estado, evidencia, riesgos) para Development/Staging/Production, con 5 contradicciones documentales identificadas (Bloque 2 ya fusionado sin reflejar; desviación de la decisión D4 — Spark aprobado vs. `billingEnabled: true` real; diseño de flavors nativos del Documento 15 vs. implementación real por entry points Dart; CORS del backend configurado para un Hosting de Development que todavía no existe; Documento 22 desactualizado respecto a Cloud Run/Cloud SQL ya desplegados) y evaluación explícita de las 10 puertas obligatorias del Documento 15 §12 (4 cumplidas, 3 parciales, 3 sin cumplir para Development, incluida la Puerta E de multiplataforma, declarada sin suavizar: Android de Development no verificado). Veredicto: **`T-F0.2`/`C1` permanece abierta** — no se declara su cierre formal; el Documento 23 sección 9 enumera exactamente los 6 puntos pendientes para reconsiderarlo. Sin cambios de código, Firebase, GCP ni infraestructura en este bloque. |
| 2026-08-03 | **T-F0.2 Bloque 4** — auditoría de cierre de solo lectura contra Documento 15, Documento 23, `PROJECT_STATUS.md`, `BACKLOG_MAESTRO.md` y los diffs reales de PR #22/#23/#24. Reconfirma el veredicto del Bloque 3 sin hallazgos que lo alteren (`origin/main` no cambió entre ambos bloques): 4 puertas cumplidas (C, F, G, I), 3 parciales (A, D, J), 3 sin cumplir (B, E, H). Concluye que los 6 pendientes son mayormente de decisión/documentación de bajo esfuerzo, salvo Puerta E (Android) y Puerta H (CI/CD), que ameritan bloques propios. Sin cambios de archivos, ramas, commits ni infraestructura. |
| 2026-08-03 | **T-F0.2 Bloque 5A.0** — auditoría de solo lectura del `applicationId` de Android Production. Hallazgo: `com.ridepro.app.YOUR_APPLICATION_ID` es un placeholder accidental (nace como scaffold el 2026-07-21, commit `7b5a238`; se conecta a un proyecto Firebase real el 2026-07-23, commit `e7f1793`, sin corregirse antes). El Documento 15 (D3, 2026-07-25) aprobó `com.ridepro.app` como reemplazo pero nunca se ejecutó — contradicción documental nueva (el propio Documento 15 lo da por "ya fijado"). Sin evidencia de distribución real bajo el placeholder actual. Recomendación: avanzar Development sin esperar la corrección de Production (aplicable de forma totalmente desacoplada vía `applicationId` absoluto por flavor); tratar la corrección de Production como bloque independiente (Bloque 5C, propuesto). Sin cambios de archivos, Firebase ni GCP. |
| 2026-08-03 | **T-F0.2 Bloque 5A** — Android Development Foundation. App Android nueva registrada en `ridepro-development` (`com.ridepro.app.dev`, "Korixa Android Development"); flavor Gradle `development` con `applicationId` absoluto (Production sin cambios); `google-services.json` propio por source set; `firebase_options_development.dart` con soporte Android real (Web sin cambios, iOS explícitamente pendiente); `firebase.json` actualizado sin alterar la referencia de Production. Validado: `flutter analyze --fatal-infos` (mismos 12 issues preexistentes en `origin/main`, cero nuevos), `flutter test` (248/6, idéntico al baseline), `flutter build apk --release` exitoso para ambos flavors con `applicationId` verificado exacto y auditoría de aislamiento del APK en ambas direcciones sin referencias cruzadas. Google Sign-In Android Development queda explícitamente pendiente de SHA-1/SHA-256. Dos incidencias de entorno de esta máquina (no del código) resueltas durante el bloque: bloqueo de Control de Aplicaciones de Windows sobre `impellerc.exe` (resuelto por el propietario) y límite de longitud de ruta de Windows en un build `--release` (resuelto compilando una copia en ruta corta, sin mover la rama/worktree real). Puerta E del Documento 15 §12 pasa de "no cumplida" a "parcial" para Development. `T-F0.2`/`C1` sigue sin declararse cerrada. Detalle completo en `docs/audits/AUDITORIA_FINAL/24_ANDROID_DEVELOPMENT_FOUNDATION.md`. |
| 2026-08-03 | **PR #25** (`feat/tf02-android-dev-foundation-20260803`, T-F0.2 Bloque 5A) integrado a `main` (merge commit `c3d95bdec56f44aa136387d4335fe0dee59aa966`) — 5/5 checks en verde (`Backend`, `Firestore`, `Flutter`, `iOS — build`, `iOS — simulator smoke test`), ninguno nuevo disparado al marcar Ready for review. |
| 2026-08-03 | **T-F0.2 Bloque 6** — alias nombrados en `.firebaserc`: `development` → `ridepro-development`, `production` → `ridepro-dbafe`; `default` (`demo-ridepro-security-tests`) sin cambios. Auditoría previa confirmó que ningún workflow de CI ni script del repositorio depende de la resolución de `.firebaserc` (`firebase/rules-tests/package.json` usa `--project=` explícito, no `firebase use`). Validado: `.firebaserc` sintácticamente válido; `firebase use development`/`production` resuelven correctamente; `firebase use default` falla, confirmado como comportamiento preexistente (proyecto ficticio de emulador, nunca resoluble por esa vía) idéntico antes y después del cambio. Cierra la Puerta B del Documento 15 §12 para Development y Production. Único pendiente puro de los 3 originales del Bloque 3: Puerta H (CI/CD). Detalle completo en `docs/audits/AUDITORIA_FINAL/25_FIREBASERC_ALIASES.md`. |
| 2026-08-03 | **Corrección de deadlock en `EquipmentRepository.applyDefault()`** (área `C2`, backend — sin relación con T-F0.2) — hallazgo detectado durante la investigación de un fallo de CI en la PR #26. `SELECT ... FOR UPDATE` sin `ORDER BY` sobre el grupo `(user_id, category_code)` permitía que transacciones concurrentes adquirieran los locks de esas filas en orden distinto, produciendo un deadlock real de PostgreSQL (`deadlock detected`, confirmado en el log de CI) bajo escritura concurrente. Corregido en la rama `fix/equipment-applydefault-deadlock-20260803` (commit `10bd74b04ff1439bad36fb51ad7996076032fe68`, padre `c3d95bdec56f44aa136387d4335fe0dee59aa966`) agregando `ORDER BY id` a esa consulta — un solo archivo, una línea. Evidencia empírica directa: 8/8 reproducciones del deadlock real sin el fix y 10/10 corridas limpias con el fix, bajo un escenario de estrés de 25 ítems concurrentes (el test original de 5 no reprodujo el fallo localmente de forma consistente, comportamiento esperado por ser timing-dependiente). Suite completa sin regresiones: 122/122 unitarias, 86/86 e2e. Desarrollado en una rama completamente independiente de la PR #26 (T-F0.2 Bloque 6), sin cherry-pick ni mezcla — **pendiente de push, PR y fusión**, no ejecutados en esta tarea. Detalle completo en `docs/audits/AUDITORIA_FINAL/26_EQUIPMENT_DEADLOCK_FIX.md`. |
| 2026-08-04 | **PR #29** (`feat/tf03-docker-compose-20260804`, `T-F0.3`) integrado a `main` (merge commit `2d8e5751931c2a76a300dcaf7a7ee812ad19a2d0`): `docker-compose.yml` para un entorno local reproducible (Postgres con healthcheck, generación idempotente de claves JWT, aplicación de migraciones, backend, y perfil `test` aislado para la suite e2e). `T-F0.3` pasa de abierta a **cerrada**. |
| 2026-08-05 | **PR #30** (`feat/tf04-redis-throttler-20260805`, `T-F0.4`) integrado a `main` (merge commit `ce54cb5fd769177e32a4fa3feb8d144bee1d91b5`): storage del rate limiter migrado de memoria a Redis (`RedisThrottlerStorage`, `ThrottlerModule.forRootAsync`), validado con una prueba de integración de storage y una prueba de aceptación multiinstancia (dos apps Nest reales compartiendo el mismo contador vía Redis). `T-F0.4` pasa de abierta a **cerrada**. |
| 2026-08-06 | **PR #31** (`feat/tf02-android-signin-sha-20260806`) integrado a `main` (merge commit `1c4e3b3eeacb38d9b886a54785d2b2c3add1b407`): agrega a `android/app/src/development/google-services.json` el cliente OAuth Android correspondiente al SHA-1 debug ya registrado en Firebase para `com.ridepro.app.dev`/`ridepro-development`; `package_name`, `project_id`, `mobilesdk_app_id` y `api_key` sin cambios; Production no tocado. |
| 2026-08-07 | **PR #33** (`fix/ci-ios-dynamic-bundle-id-20260807`) integrado a `main` (merge commit `05114fb36ff20c6fdc4541170c76ef43ce6dac91`, mismo día, **antes** que PR #32): actualiza `.github/workflows/ios-simulator-smoke.yml` para obtener `CFBundleIdentifier` dinámicamente vía `PlistBuddy` en vez de un valor hardcodeado, quedando compatible con el cambio de Bundle ID de iOS que se integraría poco después mediante PR #32. Sin cambios de Firebase ni de Bundle ID en este PR. |
| 2026-08-07 | **PR #32** (`feat/firebase-ios-production-config-20260806`) integrado a `main` (merge commit `4cca134dd16509fa48bdc511083595aa27a234ad`, mismo día, **después** de PR #33): renombra el Bundle ID de iOS Production de `com.ridepro.app` a `com.korixa.app`; integra `GoogleService-Info.plist`/`firebase_options.dart` de iOS Production actualizados; proyecto Firebase sin cambios (`ridepro-dbafe`). |
| 2026-08-08 | **PR #34** (`feat/tf02-ios-development-20260807`) integrado a `main` (merge commit `f1646958612dd9b0054d457c030509ab70741988`): separación completa iOS Development/Production vía Xcode Configurations (`Debug-development`/`Release-development`/`Profile-development`) y scheme `development`; Bundle ID `com.korixa.app.dev`; Firebase iOS Development registrado en `ridepro-development`; workflow `ios-build.yml` con matriz Production/Development. 6/6 checks verificados: 3 de `ci.yml` (Backend, Firestore, Flutter), 2 de `ios-build.yml` (Production, Development), 1 de `ios-simulator-smoke.yml`. |
| 2026-08-09 | **Evidencia runtime/local, sesión de validación del 2026-08-09 (no proveniente de GitHub/CI) — Google Sign-In en Android Development:** SHA-256 del certificado debug (`~/.android/debug.keystore`, alias `androiddebugkey`) verificado localmente y confirmado ya registrado en Firebase junto al SHA-1 (App ID `1:1020003121433:android:cb71425e96b4fe430f6d88`); `flutter run --flavor development -t lib/main_development.dart` ejecutado en emulador Android con Google Play (API 34); login real de Google completado por el propietario; app regresó autenticada; confirmado por consulta de solo lectura a Firebase Authentication de `ridepro-development` (Identity Toolkit API): usuario con provider `google.com`, `lastLoginAt` `2026-08-09T20:52:04.299Z` (correo/UID no expuestos completos, sin tokens). Durante la validación se verificó mediante ADB que la actividad en primer plano era `com.ridepro.app.dev/com.ridepro.app.MainActivity`. La suite de reglas de Firestore de CI sigue probando exclusivamente contra el emulador. |
| 2026-08-11 | **Ratificación D4 / cierre de Puerta A (Documento 15: decisión D4 histórica; criterio de Puerta A en §12) por decisión explícita del propietario:** la decisión histórica D4 (aprobada 2026-07-25 — "Development en Spark") queda **supersedida únicamente para Development**, preservada íntegra como registro histórico en el Documento 15 (no editado). Causa: Development incorporó infraestructura real de Cloud Run (`ridepro-backend-dev`) y Cloud SQL (`ridepro-backend-dev-pg`), desplegadas y validadas desde 2026-07-27/28; T-F0.2 Bloque 2 (2026-08-02) conectó la aplicación Development a esa infraestructura ya existente. Ambos servicios de GCP requieren Cloud Billing vinculado, incompatible con la intención original de Spark; `billingEnabled: true` fue detectado el 2026-08-02 sin ratificación en su momento. **Nueva decisión vigente:** `ridepro-development` mantiene Cloud Billing habilitado / Firebase Blaze mientras Cloud Run y Cloud SQL formen parte de su arquitectura — revalidado en esta sesión (`gcloud billing projects describe ridepro-development` → `billingEnabled: True`). **Staging no queda decidido ni afectado** — su plan (Spark vs. Blaze) sigue pendiente hasta su creación/evaluación. Presupuesto de alertas ("presupuesto mensual korixa", 50%/90%/100%) reportado visualmente por el propietario, no revalidado por CLI; es monitoreo, no un límite automático de gasto. Con este registro, la desviación queda **formalmente justificada y registrada** — Puerta A pasa de **PARTIAL** a **PASS**. `T-F0.2`/`C1` sigue sin declararse cerrada. |
| 2026-08-13 | **Puerta D — validación real de reglas de Firestore (D4), ejecutada en una sesión operativa de trabajo, sin PR ni commit propio:** runner Node.js usando exclusivamente Firebase Client SDK (nunca Admin SDK) como mecanismo de decisión de PASS/FAIL, ejecutado contra el proyecto real `ridepro-development`. 28/28 comportamientos canónicos de `firebase/rules-tests/firestore.rules.test.js` verificados con tokens de usuario final reales; un canal privilegiado separado (REST + `gcloud auth print-access-token`) se usó únicamente para fixtures/cleanup/verificación posterior, nunca como criterio de PASS/FAIL. Cleanup verificado de forma independiente tras el DELETE (no solo confiado a la respuesta HTTP): 0 usuarios de Firebase Authentication residuales, 0 documentos de Firestore residuales. **Esta ejecución no tiene PR ni commit asociado** — ocurrió íntegramente dentro de una sesión de trabajo, sin tocar el repositorio; esta misma entrada es, a partir de ahora, la fuente formal que preserva esa evidencia dentro de `PROJECT_STATUS.md`. **`git log` por sí solo no puede reconstruir esta ejecución** — no existe commit, rama ni artefacto versionado que la respalde; la trazabilidad depende de este registro documental. Puerta D pasa de **Parcial** a **Cumplida**. |
| 2026-08-13 | **Puerta F — redeploy de `main` a Cloud Run Development, ejecutado en sesión operativa de trabajo:** imagen construida desde un worktree aislado fijado exactamente en `main` (`173e8f5638cc90f2cedecc77166d494038fb69b0`, verificado limpio y sin desviación antes del build), publicada a Artifact Registry (digest `sha256:c1bf157aa0eed4bcfc522407d0e376df81c2691398e0922346c810068e67ce9e`), desplegada como revisión `ridepro-backend-dev-00008-ddl` con `BACKEND_ENVIRONMENT=development` como único cambio de configuración nuevo (service account, secretos, Cloud SQL, `TRUST_PROXY`, Firebase — preservados, verificados de forma sanitizada). `Ready=True`, 100% de tráfico, `GET /v1/health` → `200 {"status":"ok","database":"connected"}`, 0 logs `ERROR` en la revisión nueva. Precondición: `SERVICE_MAX_INSTANCES=1` fijado a nivel de servicio (`--max=1`) como guard operativo antes del deploy. Puerta F se reconfirma **Cumplida**, ahora corriendo el código real de `main` (la revisión anterior, `00007-llf`, era previa a la migración de Redis/`BACKEND_ENVIRONMENT`). Sin PR ni commit propio — mutación de infraestructura de sesión, registrada aquí. |
| 2026-08-13 | **Puerta G — verificación directa contra Cloud SQL Development, en sesión operativa de trabajo:** conexión real vía Cloud SQL Auth Proxy efímero en un contenedor Docker descartable (token OAuth y credenciales de Secret Manager mantenidos solo en memoria del proceso, nunca escritos a disco ni impresos, limpiados al finalizar). `current_database=ridepro_dev`, `current_user=ridepro_backend_dev`. Tabla de tracking `pgmigrations` (node-pg-migrate, mecanismo real del proyecto): 5/5 migraciones aplicadas, nombres exactamente iguales a los 5 archivos de `backend/migrations/`, sin huérfanos ni pendientes. Esquema: 11 tablas, consistentes con las 5 migraciones, sin residuos visibles. Puerta G se reconfirma **Cumplida**, ahora con evidencia en vivo (antes se apoyaba en documentación de `fase_4_1`/`fase_4_2`). Sin PR ni commit propio — verificación de sesión, registrada aquí. |
| 2026-08-13 | **PR #38** (`fix/tf02-dev-memory-throttling-20260811`) integrado a `main` (merge commit `173e8f5638cc90f2cedecc77166d494038fb69b0`): `BACKEND_ENVIRONMENT` como identidad explícita del entorno del throttler (deliberadamente distinta de `NODE_ENV`/`FIREBASE_PROJECT_ID`); Development sin `REDIS_URL` cae a throttling en memoria; cualquier otro entorno (Staging, Production, ausente o desconocido) sin `REDIS_URL` falla cerrado al arrancar, nunca se degrada en silencio. Redis real se preserva sin cambios para Staging/Production y para los tests de storage/multi-instancia ya existentes. Es la base de código que se desplegó en la entrada de Puerta F de hoy. |
| 2026-08-13 | **PR #39** (`feat/tf02-gate-h-backend-build-validate-20260813`) integrado a `main` (merge commit `5679e63571d86dc18c3c3c82926c45c1083cfd65`): primer paso real de CI/CD hacia Development — `.github/workflows/backend-build-validate.yml` construye `./backend` en GitHub Actions real (mismo comando `docker build --no-cache` ya usado en el deploy manual de la entrada de Puerta F de hoy), disparado por `workflow_dispatch` y por cambios en `backend/**`/el propio workflow. Sin credenciales GCP, sin `google-github-actions/auth`, sin Artifact Registry, sin `docker push`, sin Secret Manager, sin IAM, sin capacidad de despliegue. Validado con `actionlint` (repositorio montado `:ro`, 0 hallazgos) y 4/4 checks reales en el propio PR (`Backend — docker build`, `Backend — migración + e2e`, `Firestore — reglas`, `Flutter — analyze + test`). Puerta H pasa de **No iniciada** a **Parcial** — falta publicación autenticada a Artifact Registry y deploy verificando `projectId` antes de publicar, lo que requeriría diseñar Workload Identity Federation (una escritura de IAM), no autorizada todavía. |
| 2026-08-13 | **Reconciliación documental de `T-F0.2`/`C1` — corrección de deriva:** este documento no se actualizaba desde 2026-08-11 (PR #37), pese a 2 PRs adicionales (#38, #39) y evidencia de sesión de las Puertas D, F y G ocurrida después sin persistir aquí. Se reconcilió el estado de las 10 puertas del Documento 15 §12 contra `origin/main` = `5679e63571d86dc18c3c3c82926c45c1083cfd65`, cruzando Git/GitHub (PRs y commits reales) y la evidencia runtime documentada en las entradas de hoy: **A, B, C, D, E, F, G — Cumplida. H — Parcial (avanzada hoy, PR #39). J — Parcial (sin cambios; documentado, nunca ensayado contra infraestructura real). I — Cumplida con esta misma actualización.** El Documento 23 (`docs/audits/AUDITORIA_FINAL/23_MATRIZ_FORMAL_ENTORNOS.md`, redactado el 2026-08-03) **no se modifica** — permanece como fotografía histórica válida de esa fecha; este documento es la fuente viva y vigente. `T-F0.2`/`C1` sigue sin declararse cerrada formalmente: quedan exactamente 2 puertas abiertas — **H** (publicación autenticada a Artifact Registry + deploy con verificación de `projectId`, requiere diseñar IAM/Workload Identity Federation antes de ejecutar nada) y **J** (ensayo real de rollback contra Development, ya diseñado — shift de tráfico entre `ridepro-backend-dev-00008-ddl` y `ridepro-backend-dev-00007-llf` — pendiente de autorización). Ninguna acción de las dos se ejecuta sin autorización explícita independiente. |
| 2026-08-13 | **Puerta J — ensayo real de rollback en Cloud Run Development, ejecutado en sesión operativa de trabajo (sin PR/commit propio, registrado aquí):** servicio `ridepro-backend-dev` (proyecto `ridepro-development`, región `southamerica-east1`). Preflight de solo lectura confirmó: revisión vigente `ridepro-backend-dev-00008-ddl` con 100% de tráfico y `Ready=True`; revisión inmediatamente anterior `ridepro-backend-dev-00007-llf` (por orden real de `creationTimestamp`, no de memoria) también `Ready=True`; sin una tercera revisión recibiendo tráfico; `SERVICE_MAX_INSTANCES=1`; servicio saludable (`GET /v1/health` → `200`) antes de tocar nada. **Rollback:** `gcloud run services update-traffic --to-revisions=ridepro-backend-dev-00007-llf=100` — tráfico movido 100% a la revisión anterior; confirmado `Ready=True`, `GET /v1/health` → `200 {"status":"ok","database":"connected"}`, 0 logs `severity>=ERROR` en los 15 minutos posteriores. **Restore:** `gcloud run services update-traffic --to-revisions=ridepro-backend-dev-00008-ddl=100` (exit code 0) — tráfico devuelto 100% a la revisión original; confirmado `Ready=True`, `GET /v1/health` → `200 {"status":"ok","database":"connected"}`, 0 logs `ERROR`. **Post-restore, sin deriva de configuración detectable:** misma imagen (`sha256:c1bf157aa0eed4bcfc522407d0e376df81c2691398e0922346c810068e67ce9e`), `BACKEND_ENVIRONMENT=development` sin cambios, `DATABASE_URL` sigue vía secret reference, service account sin cambios (`ridepro-backend-dev-sa@ridepro-development.iam.gserviceaccount.com`), Cloud SQL sin cambios, Redis no tocado, IAM no tocado, `SERVICE_MAX_INSTANCES=1` sin cambios, ninguna revisión creada ni eliminada — únicamente se movió tráfico, dos veces, en ambas direcciones. **Sin PR ni commit propio** — la mutación de infraestructura ocurrió en la sesión; esta entrada es la fuente formal que preserva la evidencia. Puerta J pasa de **Parcial** a **Cumplida**. Único pendiente real de `T-F0.2`/`C1`: **Puerta H**. |
| 2026-08-15 | **PR #42** (`feat/tf02-gate-h-wif-artifact-push-20260815`) integrado a `main` (merge commit `c7ae222b10750661a032163a313962a72eca9ad4`): primer workflow con identidad cloud real hacia Development — `.github/workflows/backend-deploy-development.yml`. Infraestructura GCP/GitHub creada en la misma sesión, en bloques H0-H4 pequeños y reversibles: **H0** protección de la rama `main` (PR obligatorio, 3 checks requeridos, `enforce_admins=true`); **H1** Workload Identity Pool `github-actions-pool`; **H2** Provider OIDC `github-actions-provider` (issuer `https://token.actions.githubusercontent.com`, `attribute-condition` restringido a `repository_owner`+`repository`+`ref=refs/heads/main`+`workflow_ref` del archivo exacto del workflow); **H3** service account dedicada `ridepro-github-deployer@ridepro-development.iam.gserviceaccount.com` (nunca la SA runtime); **H4** 4 IAM bindings de mínimo privilegio a nivel de recurso, confirmando que el binding preexistente `allUsers→run.invoker` de Cloud Run no se tocó. El workflow en este PR: trigger únicamente `workflow_dispatch`, autenticación WIF sin JSON key, verificación de `projectId` antes de publicar, build+push a Artifact Registry con tag inmutable `github.sha` — deliberadamente sin ningún paso de deploy a Cloud Run todavía. Ejecutado una vez de extremo a extremo antes de fusionar (run `31917884315`, `SUCCESS`): WIF+build+push confirmados, sin tocar Cloud Run. |
| 2026-08-15 | **PR #43** (`feat/tf02-gate-h-cloud-run-deploy-rollback-20260815`) integrado a `main` (merge commit `c2b6f260d2b79a49c8db335991b09abdea6a56ab`): extiende el workflow de PR #42 con el deploy real a Cloud Run, controlado y reversible. Flujo: capturar `PREVIOUS_REVISION` (fail-closed si el tráfico actual no es exactamente 1 revisión al 100%) → `gcloud run deploy --image=...@DIGEST --no-traffic` → identificar `NEW_REVISION` sin ambigüedad → validar la candidata (Ready=True, imagen distinta a la previa, 0% de tráfico) **antes** de mover nada → `update-traffic` explícito a `NEW_REVISION=100` → verificar el split → health check acotado (`/v1/health`, HTTP 200 + `status=ok` + `database=connected`) → confirmar ausencia de drift → **rollback condicionado** (nunca `if: failure()` genérico, solo si el tráfico pudo haberse movido; si se ejecuta, el job siempre termina en `FAILURE`). `concurrency: {group: cloud-run-deploy-development, cancel-in-progress: false}` evita dos `workflow_dispatch` simultáneos. Auditoría final de solo lectura previa al merge: `VERDICT = PASS WITH WARNINGS` — único hallazgo no bloqueante: la validación HTTP de `/v1/health` ocurre después del switch de tráfico, respaldada por el rollback automático. |
| 2026-08-15 | **Puerta H — primer deploy automatizado real a Cloud Run Development, `workflow_dispatch` run `31924059938` sobre `main`=`c2b6f260d2b79a49c8db335991b09abdea6a56ab`, conclusión `SUCCESS`:** 18 pasos sustantivos en `success` (WIF auth, project guard, build, push, digest, captura de estado previo, deploy `--no-traffic`, identificación y validación de la candidata, switch de tráfico, verificación, health check, no-drift-check); rollback `skipped` — no fue necesario. `PREVIOUS_REVISION = ridepro-backend-dev-00008-ddl` → `NEW_REVISION = ridepro-backend-dev-00009-fsz` (`Ready=True`, digest `sha256:835aa45ce790c27f14c4f58b827842886f9c0b2de8ed7405d0c0eb9aae859776`), 100% del tráfico movido explícitamente. `GET /v1/health` → `200 {"status":"ok","database":"connected"}`. Verificado de forma independiente: service account, Cloud SQL, `ingress`, `SERVICE_MAX_INSTANCES=1` y referencia del secreto `DATABASE_URL` idénticos a antes del deploy — sin deriva. Puerta H pasa de **Parcial** a **Cumplida**: WIF sin credenciales de larga duración, verificación explícita de `projectId` antes de publicar, y ahora un deploy real completado con éxito. **Con esto, las 10 puertas del Documento 15 §12 (A, B, C, D, E, F, G, H, I, J) quedan Cumplida para Development — `T-F0.2`/`C1` se declara formalmente CERRADA.** El mecanismo de rollback automático permanece disponible y ya fue ensayado realmente en Puerta J (2026-08-13); no fue necesario invocarlo en este primer deploy real. |
| 2026-08-16 | **Añadida la sección "0. ESTADO VIGENTE — LEER PRIMERO"** al principio de este documento (PR documental separado, sin cambios de código/infraestructura). Motivo: el historial de este documento es correctamente append-only y extenso, y una entrada histórica aislada leída fuera de contexto podía interpretarse erróneamente como un estado abierto ya superado por evidencia posterior — en particular el riesgo de que `T-F0.2`/`C1` (formalmente **CERRADA** el 2026-08-15, entrada anterior) fuera malinterpretada como abierta a partir de alguna de las ~15 entradas previas que documentan su progreso parcial en su propia fecha. La nueva sección no reescribe ni elimina ninguna entrada histórica; declara una jerarquía de fuentes de verdad y una regla explícita de no-reapertura sin `REOPEN_REASON`/`NEW_EVIDENCE`/`LAST_CLOSING_EVIDENCE`/`WHY_CLOSURE_IS_INVALID`, consistente con la regla ya existente en "Cómo actualizar este documento" (§6 más abajo). **Cierre de las Tareas Git Windows #1-#3** (auditoría de solo lectura del Git local en Windows, clasificación y preservación remota de todo el contenido local único identificado — 24 archivos de un commit local `63f61d2` y 13 archivos del working tree, ninguno reachable desde ningún remoto antes de esta preservación —, y confirmación final de respaldo): contenido preservado en las ramas `backup/local-pre-limpieza-2026-07-21` (SHA `63f61d23e1b76351fcde4d989cbd791827c9f793`, historia sin alterar) y `backup/windows-local-wip-20260816` (SHA `e5e687b600d22d77d10ce6b0a9afbdcf98237bb0`, un único commit de preservación sobre la base `f0f7809186df594ecbe60d6c4e3f13fdd17a5330`). Ambas ramas son **referencias de preservación únicamente** — no implican revisión, aprobación funcional, ni integración a `main`; su destino final queda pendiente de decisión del propietario. `main` auditado para esta tarea: `d26feddca9a0b0a75820a4988cd98e4f7b0e990a` (sin avance desde el cierre de `T-F0.2`/`C1`). |
| 2026-08-17 | **Cierre de `T-F0.1` (validación manual, Task17) y de `T-F0.5` (Fase Enforcement, PR #55), y certificación de `QG0` (Task18) — reconciliación documental de este bloque:** (1) **`T-F0.1`** — Task17 ejecutó el smoke manual pendiente (protocolo §1, Etapa 5/Puerta 3) sobre `main`=`67ca052c6304e29a00aaffbcd2268693d1374ca5`: apertura real en Chrome (Development), navegación a Wearables, Apple Health y Google Fit mostrando "no disponible en este dispositivo", sin `UnsupportedError`/crash de `Platform`/pantalla roja; la implementación de PR #9 se confirmó intacta. `T-F0.1` pasa de **PARCIAL** a **CERRADA**. (2) **`T-F0.5`** — Fase Enforcement implementada y fusionada: PR #55 (`fix: enforce T-F0.5 pagination defaults`, merge `22e7dda9f8cc12c2e644f0449cd7a7c0b1583058`, 2 commits) retira la bifurcación de los controllers de `equipment`/`workouts` para que `GET` enrute siempre por `listPaginated()`, aplicando `DEFAULT_LIMIT=50`/`MAX_LIMIT=100` incluso sin `limit`/`cursor`; body HTTP sigue siendo `ARRAY`, cursor exclusivamente en `X-Next-Cursor`; Flutter Workouts (ya migrado en un bloque previo) sigue paginando todas las páginas sin cambios. CI post-merge sobre el merge commit exacto: run `32042395180` (`Flutter — analyze + test`, `Firestore — reglas de seguridad`, `Backend — migración + e2e`, los 3 `success`) y run `32042395169` (`Backend — validar build de imagen`, `success`). `T-F0.5` pasa de **ABIERTA — sin iniciar** a **CERRADA**. (3) **Task18/`QG0`** — auditoría de solo lectura sobre `main`=`22e7dda9f8cc12c2e644f0449cd7a7c0b1583058`, sin modificar código ni infraestructura: `T-F0.1`–`T-F0.5` re-verificadas de forma independiente (lectura directa de código en `main`, `gh pr view`/`git log`/`git merge-base --is-ancestor` para cada PR de cierre, re-ejecución real de `docker compose up` para `T-F0.3`, inspección de `rate-limit-multi-instance.e2e-spec.ts` para `T-F0.4`) — las 5 quedan `PASS_CLOSED`; gate de regresión del `main` actual (mismos 2 runs de CI post-merge de PR #55) = `PASS`; sin bloqueos. **Corrección de trazabilidad de `T-F0.2`**: el cierre operacional de la Puerta H corresponde a PR #43 (merge `c2b6f260d2b79a49c8db335991b09abdea6a56ab`) + run `31924059938`; el cierre documental/formal de `T-F0.2`/`C1` corresponde a PR #44 (merge `d26feddca9a0b0a75820a4988cd98e4f7b0e990a`) — no atribuir el cierre formal a PR #43 por sí sola. Con `T-F0.1`–`T-F0.5` cerradas y el gate de regresión en verde, `QG0` (Documento `MASTER_EXECUTION_PLAN.md` §9) queda **CERTIFICADA — PASS**. Esto habilita conceptualmente el ingreso a Fase 1; **`T-F1.1` (elegir plataforma de hosting del backend para Production) permanece ABIERTA y no iniciada, sujeta a autorización explícita separada del propietario** — este bloque no la inicia ni selecciona ninguna plataforma. |
| 2026-08-17 | **`T-F1.1` — decisión formal de hosting para Production (Google Cloud Run):** tras la certificación de `QG0`, `T-F1.1` quedó esperando la decisión explícita del propietario que exige su criterio de aceptación (`BACKLOG_MAESTRO.md`). Se realizó un análisis comparativo de tres alternativas — Cloud Run, Render y VPS autogestionado — sobre dimensiones de compatibilidad arquitectónica, seguridad (en particular: Firebase Admin SDK en este backend está diseñado exclusivamente alrededor de Application Default Credentials, nunca una JSON key de cuenta de servicio, lo que Cloud Run resuelve nativamente vía el metadata server de la revisión), reutilización de lo ya construido en Development (WIF, Artifact Registry, pipeline de build→push→deploy sin tráfico→validación→switch→health check→rollback, este último ya ensayado realmente), escalabilidad y carga operativa. La recomendación técnica resultante fue Cloud Run. **El propietario seleccionó explícitamente la opción A — Google Cloud Run** como plataforma de hosting del backend para Production. Esta selección es la decisión formal de negocio que el criterio de aceptación de `T-F1.1` exige — el hecho de que Development ya operara sobre Cloud Run/Cloud SQL **no fue tomado como autorización automática**: la decisión para Production es explícita y posterior, tomada el 2026-08-17. Con esto, `T-F1.1` queda **CERRADA**. **Este bloque es exclusivamente documental**: no se creó ningún recurso de Cloud Run/Cloud SQL/Redis de Production, no se desplegó backend de Production, no se inició `T-F1.2` (pipeline de CD), y Staging permanece no iniciado. |
| 2026-08-18 | **Reconciliación de progreso de T-F1.2 hasta D1E1:** **REPO/GITHUB EVIDENCE** — PR #58 (`ci: bootstrap inert production workflows`, merge `5d8dac305ed3f954491531255f802bd2fdddaca0`) fusionado a `main`, iniciando formalmente `T-F1.2`: 4 archivos de workflow de Production versionados (`production-identity-smoke.yml`, `production-deploy.yml`, `_backend-build-publish-production.yml`, `_backend-deploy-cloud-run-production.yml`), deliberadamente inertes — sin `id-token: write`, sin `google-github-actions/auth`, sin `workflow_run` activo, sin despliegue real. **OPERATIONAL GCP EVIDENCE FROM CONTROLLED EXECUTION SESSION** (sin PR/commit propio — mutaciones de infraestructura de sesión, registradas aquí, no reconstruibles solo con `git log`): (1) control de costos de Production cerrado — Billing vinculado a `ridepro-dbafe`, Budget "Korixa Production — Monthly Budget" creado (100.000 COP/mes, umbrales 50/80/100% sobre gasto actual, sin ser límite automático de gasto); (2) 3 Service Accounts de Production creadas con capacidades separadas — `korixa-artifact-publisher`, `korixa-production-deployer`, `korixa-production-runtime` — sin roles adicionales, sin user-managed keys; (3) capa de recursos WIF cerrada — Workload Identity Pool `github-actions-pool` + 2 providers OIDC (`github-artifact-publisher`, `github-production-deployer`), cada uno con `attribute-condition` fail-closed restringido a `repository_id`/`repository_owner_id` exactos, `ref=refs/heads/main`, el reusable workflow (`job_workflow_ref`) exacto, y el caller/evento correcto — issuer `https://token.actions.githubusercontent.com`, audiencia por defecto del provider (sin audiencia custom); un incidente parcial durante la creación del provider Artifact (rechazo por límite de 32 caracteres en `display-name`, no detectado en el diseño previo) fue reconciliado sin pérdida de estado, acortando el nombre y re-congelando el contrato antes de reintentar; un segundo incidente de sesión (pérdida de respuesta tras el intento de creación del provider Deploy) se resolvió con verificación de solo lectura antes de una única re-ejecución autorizada explícitamente por el propietario, confirmando que no había quedado ningún recurso a medias; (4) capa de impersonation cerrada — exactamente 2 bindings `roles/iam.workloadIdentityUser` de mínimo privilegio (uno por SA de capacidad, usando `attribute.job_workflow_ref` como límite exacto de impersonation para evitar que un provider pudiera impersonar la SA del otro), verificados byte-exactos tras cada creación; Runtime SA sin ningún binding WIF directo; `roles/iam.serviceAccountTokenCreator` en cero bindings (confirmado innecesario para este flujo mediante inspección directa de los permisos incluidos en `roles/iam.workloadIdentityUser`). En ningún punto de esta sesión se ejecutó un intercambio OIDC real, se activó `workflow_dispatch`, se modificó ningún workflow, ni se creó infraestructura operativa de Production (Cloud Run, Cloud SQL, Artifact Registry, Secret Manager permanecen sin uso operativo). La decisión de privilegios DB de migración (rol runtime vs. rol de migración) permanece `DEFERRED`, gate de seguridad abierto. Siguiente bloque técnico: `D1F0` — Production WIF Functional Auth Activation/Smoke Final Preflight, no ejecutado ni autorizado todavía. `T-F1.2` pasa de "no iniciada" a **EN PROGRESO** — su criterio de aceptación oficial (`BACKLOG_MAESTRO.md`) todavía no está cumplido. |
| 2026-08-22 | **Reconciliación de progreso de `T-F1.2` hasta `D2B3`, con prueba real de ejecución exitosa de `build-publish`:** desde la entrada anterior (2026-08-18, `D1E1`), se fusionaron a `main` cuatro bloques adicionales con evidencia directa de PR/CI/Actions — **`D1F1`** (PR #60, `ci: activate production artifact WIF auth-only smoke`, merge `90e36f621ff4267317935d33864e1247844f813e`): activa el camino versionado del smoke WIF auth-only de Production (guard main-only sin OIDC, `id-token` concedido únicamente al job reusable, validación de `operation`/`source_sha` antes del job privilegiado, `actions/checkout` fijado con `persist-credentials=false`, vida del token de acceso 300s) — sin dispatch ni OIDC real en ese PR. **`D2B1`** (PR #61, `ci: add production artifact build-publish capability`, merge `1652c3fd483a6ba9508e5e638e0c3c28a305629a`): agrega al reusable de Production la operación `build-publish` (gated, selector manual de operación), publicando exclusivamente a `southamerica-east1-docker.pkg.dev/ridepro-dbafe/korixa-backend/api` con tags de SHA completo, provenance OCI y comportamiento fail-closed ante colisión de tag inmutable — sin ejecución en ese PR. **`D2B2`** (PR #62, `ci: harden production immutable tag precheck`, merge `1ba4e88f9f826f33bc1a45ec98ff2a44c90ea845`): corrige la causa raíz de un fallo real descubierto en la primera ejecución de `build-publish` contra Production (`run 32192988308`, `failure`, 2026-08-18T22:30 UTC) — el precheck original usaba `gcloud artifacts docker images describe` con stdout+stderr combinados y clasificaba "tag ausente" buscando la subcadena `NOT_FOUND`, pero el texto real de gcloud para una imagen/tag inexistente es `"Image not found."` (literal distinto), por lo que todo caso legítimo de primera publicación fallaba cerrado; reemplazado por `gcloud artifacts docker images list --include-tags --format=json` con stdout y stderr capturados en archivos `mktemp` separados. **`D2B3`** (PR #63, `fix: harden backend runtime for Node 24`, merge `6fd5ff023d767079d7a5a0e724161190971dab71`): sube el runtime Docker/CI del backend de Node 20 a Node 24, alinea `@types/node`, agrega overrides transitivos seguros (`multer`, `glob`, `brace-expansion`) y gates fail-closed de motor/seguridad en CI — validación local: 170 unit + 131 e2e + 28 reglas de Firestore en verde, `npm audit` runtime en HIGH=0/CRITICAL=0, NestJS mayor sin cambios (10). **PRUEBA REAL DE EJECUCIÓN EXITOSA (Actions API, no solo código fusionado):** tras el fix de `D2B2`, el run `32210700072` (`workflow_dispatch`, `2026-08-19T03:02:27Z`, operación `build-publish`) completó `success` — el job "Build & publish Production artifact" **empujó una imagen Docker real** a Artifact Registry de Production (capas confirmadas `Pushed` en el log de Actions) usando WIF con la Service Account `korixa-artifact-publisher`, sin credenciales de larga duración. Confirmado independientemente que `.github/workflows/_backend-deploy-cloud-run-production.yml` ("Backend — Production deploy bootstrap") **tiene 0 ejecuciones registradas** — el despliegue real a Cloud Run de Production no se ha disparado nunca. La decisión de privilegios DB de migración permanece `DEFERRED`. **Nota de gobernanza (sin relación con `T-F1.2`):** el workstream independiente **Korixa Night Agent (v1, 5/5 mejoras de seguridad) está formalmente cerrado** — PR #64 fusionado (`2a58551318a37ccbe4909d7e6c649f6ad6525324`) + PR #65 (puente Slack, merge `de172f19ef53e89e7f5e311461d55be7c92ad2e6`), suite en verde (1109/1109), y `main` tiene ahora **4 checks obligatorios** (`Flutter — analyze + test`, `Firestore — reglas de seguridad`, `Backend — migración + e2e`, `Night Agent — security + test`) — no es trabajo pendiente de este documento, se registra solo como estado de gobernanza vigente. `T-F1.2` **sigue EN PROGRESO** — su criterio de aceptación oficial no está cumplido. Siguiente bloque técnico: preflight/diseño de solo lectura para el primer deploy real de Production a Cloud Run, sin identificador formal asignado todavía (`NEXT_BLOCK_ID = TO_BE_ASSIGNED_DURING_DEPLOY_PREFLIGHT`) — no ejecutado ni autorizado en esta reconciliación, que es exclusivamente documental (`git status --short` limitado a este archivo). **Remediación dirigida en la misma fecha (misma PR):** corregidos dos hallazgos documentales de un audit read-only posterior sobre esta misma reconciliación — (1) el total formal del backlog se corrigió de 34 a **35** por recuento directo de encabezados en `BACKLOG_MAESTRO.md` (que conserva sin modificar su propia suma histórica subcontada, por ser fuente congelada); (2) el encabezado de este documento ya no describe `feature/d2` como rama de desarrollo activo — es ancestro completo de `main` con 0 commits propios no incluidos, y se documenta como referencia histórica únicamente. Ningún dato de `T-F1.2`/Night Agent/gobernanza reportado arriba se modificó. |
| 2026-08-22 | **Reconciliación de infraestructura real de Production (preflight de solo lectura, sin mutación):** un preflight de `CLOUD_RUN_PRODUCTION_DEPLOY_PREFLIGHT` ejecutó `gcloud ... --project=ridepro-dbafe` explícito (nunca el proyecto por defecto de la sesión, que era `ridepro-development`) y descubrió infraestructura real de Production **nunca antes documentada en este archivo**, provisionada en sesión operativa fuera de Git el mismo rango horario que la prueba de `build-publish` (run `32210700072`, 2026-08-19T03:02 UTC): **Cloud SQL** `korixa-production-postgres` (creada 2026-08-19T04:02:24Z, `RUNNABLE`, `POSTGRES_16.14`, IP privada `10.10.16.3`, red `korixa-production-vpc`, `deletionProtectionEnabled=true`, **backups automáticos deshabilitados** `settings.backupConfiguration.enabled=false`); **Redis** `korixa-production-redis` (creada 2026-08-19T04:11:05Z, `REDIS_7_2`, `READY`, BASIC 1GB, IP privada `10.149.41.147`, `authEnabled=true`); **VPC** `korixa-production-vpc` (creada 2026-08-18, peering privado `ACTIVE` hacia Cloud SQL vía Private Services Access y hacia Redis vía Direct Peering; `vpcaccess.googleapis.com` confirmada deshabilitada — intento de listar conectores devolvió `SERVICE_DISABLED`, sin confirmar el prompt de auto-habilitación que `gcloud` mostró, cero mutación); **5 secretos** en Secret Manager (`korixa-production-database-url`/`redis-url`/`jwt-private-key`/`jwt-public-key`, creados 2026-08-19T04:21 UTC, ya vinculados con `roles/secretmanager.secretAccessor` a `korixa-production-runtime@`; `korixa-production-migration-database-url`, creado por separado 2026-08-19T10:45:22Z, **sin ningún binding IAM a nadie**, consistente con la decisión de privilegios de migración `DEFERRED`); **IAM/WIF de las 3 identidades ya reportadas en la entrada del 2026-08-18** re-verificado en vivo con separación de funciones confirmada correcta (publisher: solo `artifactregistry.writer` en el repo; deployer: `run.developer` de proyecto + `artifactregistry.reader` en el repo + `iam.serviceAccountUser` sobre la SA runtime, sin acceso a secretos; runtime: cero roles de proyecto, sin binding WIF, sin poder ser impersonada desde CI); **cero llaves `USER_MANAGED`** en las 3 cuentas de servicio (todas `SYSTEM_MANAGED`). **`GitHub Environment` `production`** existe (creada 2026-08-19T04:24:45Z), política de rama restringida a `main`, sin reviewers obligatorios ni wait timer. **Cloud Run de Production: 0 servicios** — confirmado vacío, consistente con el estado ya certificado. Nada de esto fue creado ni modificado por este bloque ni por el preflight que lo originó — es evidencia de solo lectura sobre mutaciones de sesión previas, no reconstruible solo con `git log`, igual que la entrada del 2026-08-18 para las identidades. **Único archivo modificado por este bloque: `PROJECT_STATUS.md`.** `T-F1.2` sigue **EN PROGRESO**; su criterio de aceptación oficial no está cumplido. Siguiente bloque técnico: implementar (sin activar) el reusable `_backend-deploy-cloud-run-production.yml`. |
| 2026-08-22 | **PR #68 — implementación, doble remediación independiente y fusión del reusable `_backend-deploy-cloud-run-production.yml` (bloque propuesto `D3B0`, ver nota de nomenclatura abajo):** **Implementación inicial** (commit `0f95d4806af00af9b2eee0324a8c0b2943b38f67`): diseño de 4 jobs (`validate-contract`, `deploy-candidate`, `migration-gate`, `traffic-switch`) sobre `on: workflow_call` únicamente, sin tocar `production-deploy.yml`. **Auditoría independiente #1** (`KORIXA_PR68_INDEPENDENT_SECURITY_AND_DEPLOY_SEMANTICS_AUDIT`): `PASS_WITH_FINDINGS` (P0=1, P1=4, P2=4, P3=2). **Remediación 1** (commit `b3b6182b68d96c1c25aa58ca977580dc14a6c537`) corrigió los 5: detección de primer despliegue reescrita fail-closed vía `services list` + disposición explícita (ya no infiere inexistencia de un `describe` fallido); `--allow-unauthenticated`/`--no-allow-unauthenticated` retirados del deploy (el deployer carece de `run.services.setIamPolicy`, verificado en vivo); igualdad estricta de imagen post-deploy reemplazada por procedencia OCI pre-deploy; "rollback" del primer despliegue (`--to-revisions=NEW=0`, semántica no probada) eliminado por completo — `traffic-switch` pasa a omitirse como job entero para `first_deploy=true`; rollback de despliegues subsecuentes reescrito para verificar su propio resultado en vez de asumir éxito. **Auditoría independiente #2, adversarial, post-remediación** (`KORIXA_PR68_POST_REMEDIATION_INDEPENDENT_REAUDIT`), con pruebas en vivo de solo lectura (incluida una inspección real del manifiesto OCI del artefacto de Production): `PASS_WITH_FINDINGS` (P0=0, P1=0, P2=7 — 4 nuevos, 3 rebajados de la ronda anterior); **desmintió en vivo** la justificación original de la Remediación 1 para retirar la igualdad estricta de imagen (el pipeline real usa `docker build` simple, un manifiesto concreto, no un índice multiplataforma). **Remediación 2** (commit `5a3577995a79d4ef7953412a08ee2843bce17c23`) cerró los P2 resolubles por evidencia estática: `discover-service` con stdout/stderr en archivos separados + validación JSON explícita + re-filtrado exacto por `jq` sobre `.metadata.name` (ya no depende de la semántica `=` de `--filter`, que `gcloud` advirtió en vivo que está cambiando); igualdad estricta `image == immutable_ref` **restaurada** además de (no en lugar de) la procedencia OCI; tag de candidata con `run_id`/`run_attempt` además del SHA corto; reporte de política IAM con la misma separación de streams y una clasificación explícita de 3 estados; rechazo numérico de `cpu`/`memory` en cero; `cors_allowed_origins` inyectado vía `env:` en vez de interpolarse en el script. Dos preguntas quedaron **explícitamente sin resolver** (`NONPROD_EVIDENCE_REQUIRED`, nunca marcadas como resueltas por suposición): comportamiento por defecto de invocación cuando se omiten ambos flags; semántica exacta de un reintento con el mismo tag de candidata. **Precheck final** (`KORIXA_PR68_FINAL_READY_MERGE_PRECHECK`) confirmó, con evidencia en vivo de GitHub: 1 archivo modificado en las 3 rondas, CI 4/4 (`Flutter`, `Backend`, `Firestore`, `Night Agent`) en el head final `5a35779`, cero llamadores del reusable en todo el repositorio, y reconcilió el cuerpo del PR (que había quedado desactualizado tras la Remediación 2) — `PASS`. **Ready + merge**: `gh pr ready 68` seguido de un merge ordinario de 2 padres (`gh pr merge 68 --merge`, sin squash/rebase/bypass) — merge commit `7a5ec01d2dd99b5cef2188ee9e937ff633c9d342` (padre 1 `235b030e7b4ee4b9069406662010a413ce8e3c7f` = `main` pre-merge, padre 2 `5a3577995a79d4ef7953412a08ee2843bce17c23` = head del PR). **CI post-merge sobre el merge commit exacto**: run `32582352624`, evento `push`, 4/4 `success`. **Verificación de no-efecto en Production, con evidencia en vivo de GitHub tras el merge**: cero ejecuciones registradas de `_backend-deploy-cloud-run-production.yml` como destino de cualquier llamador, cero ejecuciones de `production-deploy.yml`, `production-deploy.yml` byte-idéntico a su estado anterior (diff vacío contra el `main` pre-merge), cero referencias `uses:`/`workflow_run` en todo el repositorio apuntando a este reusable. Ninguna mutación de Cloud Run/IAM/Secret Manager/Cloud SQL/Redis/VPC/Artifact Registry ocurrió durante todo este workstream (PR #68 completo: implementación, 2 remediaciones, 2 auditorías, precheck, ready, merge) — todas las verificaciones fueron `gcloud`/`docker buildx imagetools inspect` de solo lectura. **`D3B0` sigue siendo una propuesta de nomenclatura, no formal** — ningún documento autoritativo asigna todavía un identificador posterior a `D2B3`/`D2B0-R0`; se usa aquí únicamente como referencia legible, igual que en el propio comentario de cabecera del workflow. `T-F1.2` **sigue EN PROGRESO** — ver fila `T-F1.2` en la sección 0 (actualizada en este mismo bloque) para el estado vigente completo, incluidos los bloqueadores explícitamente pendientes (evidencia no-Production, diseño de migraciones, decisiones del propietario). **Único archivo modificado por este bloque de reconciliación documental: `PROJECT_STATUS.md`** — cero cambios de código, cero cambios de workflow, cero mutación de Production. |
| 2026-08-22 | **NONPROD TEST A/B + hallazgos P1 + PR #70 — cierre de los dos bloqueadores `NONPROD_EVIDENCE_REQUIRED` heredados de PR #68, y corrección de los defectos que esa misma evidencia descubrió:** **PR #69** (merge `316dbb36a33bf8f0dce0033d54f80248d9d4c9a1`) reconcilió `PROJECT_STATUS.md` tras PR #68 — exclusivamente documental. **TEST A** (`CLOUD_RUN_INVOCATION_DEFAULT_NONPROD`, laboratorio desechable en `ridepro-development`, identidades temporales con límite de privilegio igual — `roles/run.developer`, sin `run.services.setIamPolicy` — al deployer real de Production, nunca usadas contra `ridepro-backend-dev` ni Production): un deploy que omite ambos flags de invocación resultó privado por defecto (mensaje textual de `gcloud`, política IAM sin `allUsers`, anónimo `403`, autenticado `200`) — `PROVEN_BY_LIVE_NONPROD_TEST`, disposición `INVOCATION_FLAG_OMISSION = RESOLVED_PROVEN`. Revisión independiente: `PASS_WITH_FINDINGS`, único hallazgo — contabilidad de mutaciones IAM temporales reportada como 4, reconstrucción vía Cloud Audit Logs confirmó 8 operaciones reales, `REPORTING_ACCURACY ONLY` (estado permanente neto verificado en 0). **TEST B** (`CLOUD_RUN_TAG_RETRY_NONPROD`, mismo entorno desechable): un reintento EXACTO con el mismo `--tag` creó una revisión nueva y reasignó el tag de forma atómica, dejando la candidata anterior sin tag y en 0%, tráfico primario intacto — disposición `SAME_ATTEMPT_TAG_RETRY = RESOLVED_PROVEN_SAFE` (prueba el reintento exacto ensayado, no independencia universal de otras variaciones de configuración). Revisión independiente: `PASS_WITH_FINDINGS`. **TEST B descubrió, en vivo, 2 defectos P1 reales ya fusionados en PR #68**: límite real de Cloud Run `length(service_name) + length(candidate_tag) <= 46` nunca validado por el contrato; audiencia de identity token del health check de la candidata usando incorrectamente la URL etiquetada en vez de la URL canónica del servicio (confirmado también por documentación oficial de Cloud Run). La revisión independiente de la remediación encontró además que el health check post-rollback carecía de fallback autenticado (mismo origen). **PR #70** (`fix(ci): harden Cloud Run candidate tag and private health checks`; 1 commit, 1 archivo; merge ordinario de 2 padres `316dbb36a33bf8f0dce0033d54f80248d9d4c9a1`/`d79b51794ef05e03c658b9784267841a23798be8` → `e5158cd5825fde4b8bd5f154acf96d34b7d8161d`) corrigió los tres: tag de candidata rediseñado (`c-<github.run_id>-<github.run_attempt>`, ya sin `source_sha`, fuente única en `validate-contract`, con validación fail-closed del límite combinado ANTES de checkout/OIDC/auth/cloud); health check de la candidata con `REQUEST_URL` (etiquetada) y `TOKEN_AUDIENCE` (canónica) como valores distintos; mismo fallback autenticado agregado al health check post-rollback; criterios de éxito de salud sin debilitar. Reauditoría independiente: `PASS_WITH_FINDINGS` — P0=0, P1=0, P2=0, P3=1 (`CONTRACT_VALID=YES` impreso antes del step de validación de longitud combinada; cero consumidores, `REPORTING_ACCURACY`, `NON_BLOCKING`, no bloquea merge ni activación). Precheck final `PASS`. **Merge**: `e5158cd5825fde4b8bd5f154acf96d34b7d8161d`, confirmado con exactamente 2 padres. **CI post-merge**: run `32588729509`, evento `push`, intento 1, 4/4 `SUCCESS` (`Flutter`, `Firestore`, `Backend`, `Night Agent`). **Certificación postmerge independiente**: `PASS` — branch protection intacta (4 checks requeridos, `strict=true`, `enforce_admins=true`), cero ejecuciones de `production-deploy.yml`/`_backend-deploy-cloud-run-production.yml`/`_backend-build-publish-production.yml` causadas por todo este bloque, `production-deploy.yml` byte-idéntico a `main` pre-merge, cero referencias `uses:`/`workflow_run` apuntando al reusable. **Ninguna mutación de Cloud Run/IAM/Secret Manager/Cloud SQL/Redis/VPC/Artifact Registry de Production ocurrió durante todo este workstream** (TEST A, TEST B, PR #70 completo) — las únicas mutaciones reales fueron en `ridepro-development`, íntegramente desechables, íntegramente limpiadas y verificadas sin residuos. El reusable **permanece INALCANZABLE**. Decisión de privilegios DB de migración sigue `DEFERRED`. `T-F1.2` **sigue EN PROGRESO** — ver fila `T-F1.2` en la sección 0 (actualizada en este mismo bloque) para el estado vigente completo y la lista precisa de bloqueadores restantes (migraciones, guarda de frescura del llamador futuro, decisiones de producto/runtime del propietario, reviewer obligatorio, backups de Cloud SQL, diseño de promoción de primer despliegue). **Único archivo modificado por este bloque de reconciliación documental: `PROJECT_STATUS.md`** — cero cambios de código, cero cambios de workflow, cero mutación de Production. |
| 2026-08-24 | **Night Agent 7/7 — cierre técnico y operativo.** Programa de endurecimiento del Korixa Night Agent (protocolo de coordinación de un solo chat NIGHT→A→B→C), Tareas 1-7, cerrado con evidencia real de PR/CI para cada bloque. **Tarea 1** (base de endurecimiento de seguridad: gate Executor↔Auditor, taxonomía de evidencia, fase red-team) — PR #73, merge `2107b020cdc1eba0a2b3b58079c1ff6f672b9df0`. **Tarea 2** (Common Agent Protocol) — PR #75, merge `78815fd044925f8f6d3d221ab45456a680e0477f`. **Tarea 3** (preparación y separación formal de roles A/B/C) — PR #77, merge `1b9a6571e8d6adc047bb66eb707254d4e9bc9649`. **Tarea 4** (tareas/locks/handoffs/estado compartido/enforcement en runtime) — PR #78, merge `c9a19df8700f5ced25e2bd57670cb2ec7faeebdf`. **Tarea 5** (simulación integrada controlada, 18 escenarios) — PR #79, merge `551b5679feb650edc4e4513bb6f26393aa304489`. **Tarea 6** (remediación de hallazgos de simulación + gate final de metadatos de PR) — PR #80, merge `588c1d094ebedff51dba001eba6570d439d5f4a5`. **Tarea 7** (primera operación real simultánea) tuvo dos intentos reales: **Intento 1** — PR #81 (`feat/task7-first-real-simultaneous-operation-20260823`, permanece **abierto, sin fusionar**, evidencia histórica): la operación GREEN real (documentación) se ejecutó y se validó correctamente en sí misma, pero la aceptación final descubrió un defecto real del sistema — `task-orchestrator-pr-number-unrecordable-post-creation` (una sesión de tarea debía crearse antes de que su rama tuviera un commit, por lo que ningún número de PR real podía conocerse al momento de la creación) — y terminó correctamente en `HOLD_SYSTEM_DEFECT` en lugar de un PASS falso; **no se reescribe como PASS, no se le asigna el PR #83, no se fusiona.** **Hotfix** — PR #82 (`fix/night-agent-pr-identity-binding-20260823`), merge `37b05c2a0162a9090f59c5d3594208ca7acc355b`: agregó `recordPrOpened`/la capacidad `BIND_PR_IDENTITY` (exclusiva del rol NIGHT) para vincular el número de PR real *después* de su creación real, corrigiendo el defecto de raíz; propia auditoría B encontró y corrigió un hallazgo P2 real (comparación `!==` floja entre dos `null`); certificado y fusionado con CI post-merge 4/4 `SUCCESS`. **Tarea 7 Intento 2** — PR #83 (`feat/task7-attempt2-real-operation-20260824`, nueva sesión de tarea completa, sin reutilizar el lock/rama/estado del Intento 1): base `37b05c2a0162a9090f59c5d3594208ca7acc355b`, HEAD de la tarea `81c2c76c37de869dd793aedb818a9c38141e0ae8`, GREEN real = documentar en `tools/night-agent/README.md` los seis módulos de coordinación de Tareas 2/3/4/6 más el ciclo de vida `recordPrOpened`/`BIND_PR_IDENTITY` del hotfix; trabajo concurrente real detectado y preservado (worktree principal sucio en rama no relacionada + worktree de agente con lock de Git activo, ambos sin alterar); `recordPrOpened` vinculó realmente `pr_number` de `null` → `83` después de la creación real del PR; auditoría B independiente = `PASS` (P0=P1=P2=P3=0); certificación C = `PASS`; gate final de metadatos de PR sincronizado y reverificado contra un snapshot fresco del PR (hash de cuerpo byte-exacto verificado, cero marcadores obsoletos); fusionado a `main` = `0b971cc46c49bb3335cef438c503baa422659816`; **CI post-merge run `32694668074` = 4/4 `SUCCESS`** sobre ese HEAD exacto. **Limpieza operativa posterior** (tarea de solo cierre de coordinación runtime, sin cambios de código): liberación real del lock de alcance y del slot de tarea activa del Intento 2 con su owner-token legítimo; verificado sin locks fantasma (`listActiveTaskLocks` vacío) y disponibilidad del slot probada con un probe real desechable; los registros históricos del Intento 1 y del Hotfix se verificaron con hash SHA-256 idéntico antes/después — sin alteración. Resultado: `NIGHT_AGENT_OPERATIONAL_CLOSURE=PASS`, `STATE=CLOSED_CLEAN`. Ningún bloque de este cierre tocó Production, IAM, secretos, protección de rama, ni `T-F1.2` (que permanece `EN PROGRESO`, sin cambios). |
| 2026-08-24 | **`T-F1.2` — PR #85/#86, incidente #87 y hardening sistémico #88:** **PR #85** integró a `main` el caller manual-only de deploy Production (`workflow_dispatch`) con la cadena `guard → build-production-artifact → verify-build-output → deploy-production-candidate`; B/C PASS y CI requerido 4/4, pero `MIGRATION_PRECONDITION_PROVEN=NO`, por lo que no hubo deploy real. **PR #86** integró el caller manual-only de inspección read-only de Production DB, también sin ejecución durante el PR. En el primer dispatch posterior autorizado de esa inspección, GitHub rechazó la definición antes de crear un run porque tres jobs normales con `steps:` carecían de `runs-on:` (dos en el caller de inspección y uno en el caller de deploy): ningún job corrió, no hubo consulta DB, no hubo publicación/deploy y las mutaciones Production fueron 0. **PR #87** (`fix(actions): make Production DB inspection caller GitHub-valid`) fue abierto como fix acotado, pero quedó Draft y sin merge al decidirse corregir la clase sistémica del fallo. **PR #88** (`fix(ci): harden GitHub Actions workflow structure validation`) sustituyó ese enfoque: añadió validación estructural fail-closed de todos los workflows, actionlint fijado/verificado, reforzó la procedencia HEAD-bound y los gates B/C/Human, y pasó cinco rondas adversariales hasta cerrar todos los P1; resultado final B=`PASS`, C=`PASS`, P0=0/P1=0/P2=0/P3=1 no bloqueante. El Human Gate asignó canónicamente `TASK=6/6`, luego autorizó Ready y Merge por separado. Merge de #88: `caf4afbfbf70efc6306ee9bd83ff8f48feb0f599`; CI post-merge `32801653025` sobre `main`, evento `push`, 4/4 `SUCCESS`; Post-merge CI locator `32801868448` `SUCCESS`. **Estado después de este cierre:** `T-F1.2` sigue **EN PROGRESO**; callers de Production siguen manuales; `MIGRATION_PRECONDITION_PROVEN=NO`; no se ha ejecutado un deploy real de Cloud Run Production. Siguiente paso técnico: preflight de solo lectura de la inspección DB sobre el `main` vigente y autorización humana separada antes de cualquier dispatch. Esta entrada documental no cierra #87 ni #81 y no realiza mutaciones de Production. |

---

## Cómo actualizar este documento (recordatorio operativo, no forma parte del estado en sí)

1. Al cerrar cualquier tarea (Etapa 10 del ciclo de vida), agregar una fila nueva a la sección 5 con la fecha y el ID de la tarea.
2. Recalcular la sección 2 (porcentaje de avance) solo con evidencia directa revalidada — no fabricar un porcentaje global sin revisar cada unidad.
3. Mover el ítem correspondiente en la sección 4 (Backlog abierto) si su estado cambió, o actualizar "Próximos bloques" si se cerró un bloque completo.
4. Actualizar la sección 3 (Estado de infraestructura) si cambió Backend, Flutter, Docker, Firebase, CI o Tests.
5. Actualizar la fecha de última actualización y quién actualiza, en el encabezado.
6. **Protección contra reapertura no verificada:** una tarea o gate marcada pendiente en una auditoría histórica (Documento 23, cualquier entrada anterior de este mismo documento, o cualquier otro informe punto-en-el-tiempo) **no puede reabrirse** sin antes verificar si existe evidencia posterior en `main`, PRs, commits o runtime documentado que ya la haya cerrado. Antes de cualquier reapertura debe reportarse explícitamente:
   - `REOPEN_REASON =`
   - `NEW_EVIDENCE =`
   - `LAST_CLOSING_EVIDENCE =`
   - `WHY_CLOSURE_IS_INVALID =`

   Si los cuatro campos no pueden justificarse con evidencia real, **no se reabre**.
