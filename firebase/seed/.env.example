# Copiar a .env y rellenar — .env NUNCA se commitea (ver .gitignore raíz,
# *.env). Estas credenciales SOLO existen dentro del Auth Emulator local
# (localhost:9099); nunca son válidas contra el proyecto Firebase real
# (ridepro-dbafe). Ver seed_emulator.js.

QA_EMULATOR_EMAIL=qa.emulator@ridepro.local
QA_EMULATOR_PASSWORD=elegir-una-contraseña-solo-local
QA_EMULATOR_DISPLAY_NAME=QA Emulator
