{
  "name": "ridepro-firestore-rules-tests",
  "private": true,
  "version": "1.0.0",
  "description": "Tests automatizados de las reglas de seguridad de Firestore — ejecutar contra el emulador, nunca contra el proyecto real.",
  "scripts": {
    "test": "firebase emulators:exec --config ../../firebase.json --project=demo-ridepro-security-tests --only firestore \"jest --runInBand\"",
    "test:rules": "jest --runInBand"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^3.0.4",
    "firebase": "^10.12.0",
    "jest": "^29.7.0"
  }
}
